import{a as e,f as t,i as n,n as r,o as i,p as a,s as o,u as s}from"./app-rVckXADX.js";import{t as c}from"./plugin-vue_export-helper-BDNMzG2s.js";var l=JSON.parse(`{"path":"/zh/posts/notes/database/innodb-and-index.html","title":"InnoDB 与索引：一条 SQL 的数据路径","lang":"zh-CH","frontmatter":{"lang":"zh-CH","title":"InnoDB 与索引：一条 SQL 的数据路径","description":"从行与页、B+ 树、缓冲池到 redo/undo/binlog，串起一条 SQL 的完整读写路径，并说清索引在哪些情况下帮不上忙","date":"2026-09-21T00:00:00.000Z","category":["后端开发"],"tag":["数据库","索引"],"head":[["script",{"type":"application/ld+json"},"{\\"@context\\":\\"https://schema.org\\",\\"@type\\":\\"Article\\",\\"headline\\":\\"InnoDB 与索引：一条 SQL 的数据路径\\",\\"image\\":[\\"\\"],\\"datePublished\\":\\"2026-09-21T00:00:00.000Z\\",\\"dateModified\\":\\"2026-09-23T16:13:32.000Z\\",\\"author\\":[{\\"@type\\":\\"Person\\",\\"name\\":\\"vexillium\\",\\"url\\":\\"https://vexillium2.github.io\\"}]}"],["meta",{"property":"og:url","content":"https://vexillium2.github.io/zh/posts/notes/database/innodb-and-index.html"}],["meta",{"property":"og:site_name","content":"Vexilog"}],["meta",{"property":"og:title","content":"InnoDB 与索引：一条 SQL 的数据路径"}],["meta",{"property":"og:description","content":"从行与页、B+ 树、缓冲池到 redo/undo/binlog，串起一条 SQL 的完整读写路径，并说清索引在哪些情况下帮不上忙"}],["meta",{"property":"og:type","content":"article"}],["meta",{"property":"og:locale","content":"zh-CH"}],["meta",{"property":"og:locale:alternate","content":"zh-CN"}],["meta",{"property":"og:updated_time","content":"2026-09-23T16:13:32.000Z"}],["meta",{"property":"article:tag","content":"索引"}],["meta",{"property":"article:tag","content":"数据库"}],["meta",{"property":"article:published_time","content":"2026-09-21T00:00:00.000Z"}],["meta",{"property":"article:modified_time","content":"2026-09-23T16:13:32.000Z"}],["link",{"rel":"alternate","hreflang":"zh-cn","href":"https://vexillium2.github.io/zh/posts/notes/database/innodb-and-index.html"}]]},"git":{"createdTime":1790180012000,"updatedTime":1790180012000,"contributors":[{"name":"vexillium2","username":"vexillium2","email":"ntc_ip_rbm@163.com","commits":1,"url":"https://github.com/vexillium2"}]},"readingTime":{"minutes":21.22,"words":6366},"filePathRelative":"zh/posts/notes/database/innodb-and-index.md","excerpt":"\\n<p>一条 SQL 从接收到返回，会在四层结构上留下痕迹：<strong>磁盘上的页</strong>、<strong>按主键组织的 B+ 树</strong>、<strong>内存里的缓冲池</strong>、<strong>顺序追加的日志</strong>。这四层各自解决一个物理问题——数据放哪、怎么快速定位、怎么避免随机 I/O、怎么保证掉电不丢。</p>\\n<p>理解它们的顺序是&quot;从底往上&quot;：先看数据在磁盘上长什么样，再看索引如何在一堆页里定位，再看内存如何把磁盘访问变少，最后看日志如何把随机写变成顺序写。把四层拼起来，就是一条 SQL 的真实路径。</p>\\n<p>这条路径也解释了索引的边界：<strong>索引存在不等于会被使用</strong>，而&quot;不被使用&quot;有时是优化器的正确决策，有时才是设计错误。</p>"}`),u={name:`innodb-and-index.md`},d={class:`table-of-contents`};function f(c,l,u,f,p,m){let h=t(`router-link`);return s(),n(`div`,null,[l[26]||=e(`<h1 id="innodb-与索引-一条-sql-的数据路径" tabindex="-1"><a class="header-anchor" href="#innodb-与索引-一条-sql-的数据路径"><span>InnoDB 与索引：一条 SQL 的数据路径</span></a></h1><p>一条 SQL 从接收到返回，会在四层结构上留下痕迹：<strong>磁盘上的页</strong>、<strong>按主键组织的 B+ 树</strong>、<strong>内存里的缓冲池</strong>、<strong>顺序追加的日志</strong>。这四层各自解决一个物理问题——数据放哪、怎么快速定位、怎么避免随机 I/O、怎么保证掉电不丢。</p><p>理解它们的顺序是&quot;从底往上&quot;：先看数据在磁盘上长什么样，再看索引如何在一堆页里定位，再看内存如何把磁盘访问变少，最后看日志如何把随机写变成顺序写。把四层拼起来，就是一条 SQL 的真实路径。</p><p>这条路径也解释了索引的边界：<strong>索引存在不等于会被使用</strong>，而&quot;不被使用&quot;有时是优化器的正确决策，有时才是设计错误。</p>`,4),r(`nav`,d,[r(`ul`,null,[r(`li`,null,[o(h,{to:`#行与页-innodb-如何把表放进磁盘`},{default:a(()=>[...l[0]||=[i(`行与页：InnoDB 如何把表放进磁盘`,-1)]]),_:1}),r(`ul`,null,[r(`li`,null,[o(h,{to:`#行格式与行溢出-一行为什么会跨越多个页`},{default:a(()=>[...l[1]||=[i(`行格式与行溢出：一行为什么会跨越多个页`,-1)]]),_:1})]),r(`li`,null,[o(h,{to:`#页、区、段、表空间-innodb-的四层容器`},{default:a(()=>[...l[2]||=[i(`页、区、段、表空间：InnoDB 的四层容器`,-1)]]),_:1})]),r(`li`,null,[o(h,{to:`#字段类型的选择-它决定了行有多宽`},{default:a(()=>[...l[3]||=[i(`字段类型的选择：它决定了行有多宽`,-1)]]),_:1})])])]),r(`li`,null,[o(h,{to:`#b-树与索引组织表`},{default:a(()=>[...l[4]||=[i(`B+ 树与索引组织表`,-1)]]),_:1}),r(`ul`,null,[r(`li`,null,[o(h,{to:`#b-树、b-树与-lsm-三种写读权衡`},{default:a(()=>[...l[5]||=[i(`B+ 树、B 树与 LSM：三种写读权衡`,-1)]]),_:1})]),r(`li`,null,[o(h,{to:`#聚簇索引-表的数据就存在主键的-b-树里`},{default:a(()=>[...l[6]||=[i(`聚簇索引：表的数据就存在主键的 B+ 树里`,-1)]]),_:1})]),r(`li`,null,[o(h,{to:`#二级索引与回表-为什么查一个字段要多跳一次`},{default:a(()=>[...l[7]||=[i(`二级索引与回表：为什么查一个字段要多跳一次`,-1)]]),_:1})]),r(`li`,null,[o(h,{to:`#主键选型-自增、uuid-与雪花算法的代价`},{default:a(()=>[...l[8]||=[i(`主键选型：自增、UUID 与雪花算法的代价`,-1)]]),_:1})])])]),r(`li`,null,[o(h,{to:`#缓冲池-随机-i-o-是怎么被消掉的`},{default:a(()=>[...l[9]||=[i(`缓冲池：随机 I/O 是怎么被消掉的`,-1)]]),_:1}),r(`ul`,null,[r(`li`,null,[o(h,{to:`#缓冲池-页的状态与改进的-lru`},{default:a(()=>[...l[10]||=[i(`缓冲池：页的状态与改进的 LRU`,-1)]]),_:1})]),r(`li`,null,[o(h,{to:`#change-buffer-非唯一二级索引的写为什么能缓`},{default:a(()=>[...l[11]||=[i(`Change Buffer：非唯一二级索引的写为什么能缓`,-1)]]),_:1})]),r(`li`,null,[o(h,{to:`#刷脏与双写缓冲-页什么时候写盘-写坏了怎么办`},{default:a(()=>[...l[12]||=[i(`刷脏与双写缓冲：页什么时候写盘，写坏了怎么办`,-1)]]),_:1})])])]),r(`li`,null,[o(h,{to:`#日志-为什么一次写入要记三份`},{default:a(()=>[...l[13]||=[i(`日志：为什么一次写入要记三份`,-1)]]),_:1}),r(`ul`,null,[r(`li`,null,[o(h,{to:`#wal-为什么必须先写日志再写数据页`},{default:a(()=>[...l[14]||=[i(`WAL：为什么必须先写日志再写数据页`,-1)]]),_:1})]),r(`li`,null,[o(h,{to:`#redo-与-undo-崩溃恢复和回滚各要什么`},{default:a(()=>[...l[15]||=[i(`redo 与 undo：崩溃恢复和回滚各要什么`,-1)]]),_:1})]),r(`li`,null,[o(h,{to:`#binlog-与两阶段提交-两套日志为什么必须同时存在`},{default:a(()=>[...l[16]||=[i(`binlog 与两阶段提交：两套日志为什么必须同时存在`,-1)]]),_:1})])])]),r(`li`,null,[o(h,{to:`#一条-sql-的完整路径`},{default:a(()=>[...l[17]||=[i(`一条 SQL 的完整路径`,-1)]]),_:1}),r(`ul`,null,[r(`li`,null,[o(h,{to:`#读路径-一条二级索引查询经过哪些结构`},{default:a(()=>[...l[18]||=[i(`读路径：一条二级索引查询经过哪些结构`,-1)]]),_:1})]),r(`li`,null,[o(h,{to:`#写路径-一条-update-要改几处地方`},{default:a(()=>[...l[19]||=[i(`写路径：一条 UPDATE 要改几处地方`,-1)]]),_:1})])])]),r(`li`,null,[o(h,{to:`#索引的边界-什么时候用不上、什么时候不该建`},{default:a(()=>[...l[20]||=[i(`索引的边界：什么时候用不上、什么时候不该建`,-1)]]),_:1}),r(`ul`,null,[r(`li`,null,[o(h,{to:`#最左前缀与范围截断-联合索引什么时候只用得上前半段`},{default:a(()=>[...l[21]||=[i(`最左前缀与范围截断：联合索引什么时候只用得上前半段`,-1)]]),_:1})]),r(`li`,null,[o(h,{to:`#索引列上的运算-函数、隐式转换与前缀匹配`},{default:a(()=>[...l[22]||=[i(`索引列上的运算：函数、隐式转换与前缀匹配`,-1)]]),_:1})]),r(`li`,null,[o(h,{to:`#区分度与优化器-索引存在不等于会被使用`},{default:a(()=>[...l[23]||=[i(`区分度与优化器：索引存在不等于会被使用`,-1)]]),_:1})]),r(`li`,null,[o(h,{to:`#null-与索引-唯一索引为什么不唯一`},{default:a(()=>[...l[24]||=[i(`NULL 与索引：唯一索引为什么不唯一`,-1)]]),_:1})]),r(`li`,null,[o(h,{to:`#什么时候不该建索引`},{default:a(()=>[...l[25]||=[i(`什么时候不该建索引`,-1)]]),_:1})])])])])]),l[27]||=e(`<h2 id="行与页-innodb-如何把表放进磁盘" tabindex="-1"><a class="header-anchor" href="#行与页-innodb-如何把表放进磁盘"><span>行与页：InnoDB 如何把表放进磁盘</span></a></h2><h3 id="行格式与行溢出-一行为什么会跨越多个页" tabindex="-1"><a class="header-anchor" href="#行格式与行溢出-一行为什么会跨越多个页"><span>行格式与行溢出：一行为什么会跨越多个页</span></a></h3><p>InnoDB 的页默认 16KB，表数据按行存进页里。一行在页内的布局依次是：<strong>变长字段长度列表</strong>（每个变长列记多长，超过 255 字节的用 2 字节表示）、<strong>NULL 标志位图</strong>（每个允许为 NULL 的列占 1 bit）、<strong>记录头</strong>、<strong>列值</strong>。</p><p>两个直接推论：</p><ul><li><strong>允许为 NULL 的列本身不占数据空间</strong>（只在标志位图里占 1 bit），但它会在索引与约束上留下代价。</li><li><strong>当一行太大放不进一个页，InnoDB 会把大字段挪到溢出页</strong>（off-page），行内只留一个 20 字节指针。这就是 <code>TEXT</code>/<code>BLOB</code>/超长 <code>varchar</code> 的真实代价：它不只是占空间，而是让<strong>同一行的读取变成两次 I/O</strong>，并且把该行所在页能容纳的行数压下来。</li></ul><p>一条经验判据比背数字有用：<strong>页要能装下足够多的行</strong>。一页如果只能放一两行，扫描这张表的 I/O 次数就与行数同阶，缓冲区也更容易被冲掉。所以设计上常见的做法是<strong>把大字段拆到独立的副表</strong>，用主键一一对应——主表变窄之后，每页行数上升，索引与范围扫描都更快。</p><h3 id="页、区、段、表空间-innodb-的四层容器" tabindex="-1"><a class="header-anchor" href="#页、区、段、表空间-innodb-的四层容器"><span>页、区、段、表空间：InnoDB 的四层容器</span></a></h3><p>从大到小四层，理解它们的关键是<strong>各自承担什么分配职责</strong>：</p><table><thead><tr><th>层</th><th>大小</th><th>职责</th></tr></thead><tbody><tr><td>表空间（tablespace）</td><td>一个或多个文件</td><td>物理文件的容器：系统表空间 <code>ibdata1</code>、独立表空间 <code>表名.ibd</code>、通用表空间、undo 表空间、临时表空间</td></tr><tr><td>段（segment）</td><td>按需增长</td><td>给一个逻辑对象（如一个索引）分配空间，每个索引通常占两个段：叶子节点段与非叶子节点段</td></tr><tr><td>区（extent）</td><td>1MB = 64 个 16KB 页</td><td>空间分配的<strong>基本单位</strong>，按区申请以减少碎片</td></tr><tr><td>页（page）</td><td>16KB</td><td>磁盘与内存之间传输的<strong>最小单位</strong>；一页可能会承载多行</td></tr></tbody></table><p>要抓住的两点：<strong>页是 I/O 单位，区是分配单位</strong>。读取永远以页为粒度，所以&quot;只查一行&quot;和&quot;查同一页里的一百行&quot;在 I/O 上几乎同价；反过来，只多要一行但那一行落在另一个页上，就要再付一整页的代价。</p><h3 id="字段类型的选择-它决定了行有多宽" tabindex="-1"><a class="header-anchor" href="#字段类型的选择-它决定了行有多宽"><span>字段类型的选择：它决定了行有多宽</span></a></h3><p>字段类型不是&quot;哪个看起来更合适&quot;的问题，而是<strong>行宽</strong>问题——行宽决定每页放多少行，进而决定整条路径上每一层的效率。</p><ul><li><strong><code>char</code> 与 <code>varchar</code></strong>：定长列不需要长度列表项，但会用空格补齐；变长列省空间，代价是每行多 1 到 2 字节的长度记录。字符串长度基本固定的才用 <code>char</code>。</li><li><strong>整数类型</strong>：按取值范围选最小够用的。唯一例外是主键——它会被复制进每一个二级索引，所以主键要留足空间。</li><li><strong><code>decimal</code> 与浮点</strong>：金额必须用 <code>decimal</code>。二进制浮点无法精确表示大多数十进制小数，<code>0.1 + 0.2</code> 不等于 <code>0.3</code>，用它做金额比较会得到错误结果。这是<strong>表示能力</strong>问题，不是精度调优问题。</li><li><strong>时间类型</strong>：<code>DATETIME</code> 存的是 <strong>5 字节加上小数秒的增量</strong>（<code>DATETIME(0)</code> 5 字节，<code>(6)</code> 才 8 字节），<strong>原样存储、不做时区转换</strong>；<code>TIMESTAMP</code> 存的是 UTC 秒数，<strong>4 字节加上小数秒增量</strong>（<code>(0)</code> 4 字节，<code>(6)</code> 7 字节），读取时按会话时区换算，范围上限到 2038 年。</li><li><strong>两者的差别不是精度而是语义</strong>：需要按用户时区自动换算、或者跨时区部署时，<code>TIMESTAMP</code> 的换算能力有用；需要原样记录一个&quot;墙上时间&quot;、或者要跨过 2038 年，就必须用 <code>DATETIME</code>。</li></ul><h2 id="b-树与索引组织表" tabindex="-1"><a class="header-anchor" href="#b-树与索引组织表"><span>B+ 树与索引组织表</span></a></h2><h3 id="b-树、b-树与-lsm-三种写读权衡" tabindex="-1"><a class="header-anchor" href="#b-树、b-树与-lsm-三种写读权衡"><span>B+ 树、B 树与 LSM：三种写读权衡</span></a></h3><p>索引结构的选择本质是在<strong>读放大、写放大、空间放大</strong>三者之间取舍：</p><table><thead><tr><th>结构</th><th>等值查找</th><th>范围扫描</th><th>写入</th><th>代表</th></tr></thead><tbody><tr><td>哈希</td><td>一次</td><td>不支持</td><td>追加/桶分裂</td><td>Memory 引擎、自适应哈希索引</td></tr><tr><td>B+ 树</td><td>树高次</td><td>叶子链表顺序扫描</td><td>原地更新，随机 I/O，页分裂</td><td>InnoDB</td></tr><tr><td>LSM 树</td><td>多层查找，读放大</td><td>层内有序，可合并</td><td>顺序追加，写放大转移到后台合并</td><td>RocksDB、TiKV</td></tr></tbody></table><p>B+ 树相对 B 树的关键差别：<strong>只有叶子节点存完整数据，非叶子节点只存键和指针</strong>。同样大小的非叶子页因此能放更多键，树更矮；叶子之间用双向链表相连，范围扫描只要找到起点然后顺序右移，不必回到上层做中序遍历。这正是&quot;按范围查、按顺序取&quot;这类需求在关系型数据库上高效的根本原因。</p><p>哈希不支持范围与排序，所以它只能做补充（InnoDB 的自适应哈希索引就是在 B+ 树之上、针对等值查询建的旁路），不能替代 B+ 树。</p><h3 id="聚簇索引-表的数据就存在主键的-b-树里" tabindex="-1"><a class="header-anchor" href="#聚簇索引-表的数据就存在主键的-b-树里"><span>聚簇索引：表的数据就存在主键的 B+ 树里</span></a></h3><p>InnoDB 把整张表的数据按主键顺序组织在 B+ 树的叶子里，这种表叫<strong>索引组织表</strong>。<strong>聚簇索引就是这张表本身</strong>，不是它上面的一个附属结构。</p><p>主键的确定顺序是：</p><ol><li>显式定义的主键；</li><li>没有主键，就取第一个<strong>非空唯一索引</strong>；</li><li>再没有，InnoDB 自动生成一个 6 字节的隐藏列作为聚簇索引键。</li></ol><p>这个机制有一个容易被忽略的后果：<strong>你选的不是&quot;主键&quot;，而是&quot;整张表的物理组织顺序&quot;</strong>。换成第 2 或第 3 种情况时，表的数据顺序由那个唯一索引甚至隐藏列决定，这可能与你的查询模式完全无关。</p><h3 id="二级索引与回表-为什么查一个字段要多跳一次" tabindex="-1"><a class="header-anchor" href="#二级索引与回表-为什么查一个字段要多跳一次"><span>二级索引与回表：为什么查一个字段要多跳一次</span></a></h3><p><strong>二级索引的叶子节点存的是&quot;索引列值 + 主键值&quot;</strong>，不是行的物理地址。原因是聚簇索引会因页分裂而移动数据，物理地址不稳定，而主键稳定。</p><p>所以通过二级索引查非索引列，需要两次树查找：</p><ol><li>在二级索引的 B+ 树里找到目标键，取出主键值；</li><li>用主键值回到聚簇索引的 B+ 树里取出整行。这一步叫<strong>回表</strong>。</li></ol><p>回表的次数等于<strong>匹配到的二级索引记录数</strong>（在没有条件下推的优化时），每一次都可能是一次随机 I/O。这就是&quot;索引没少用，但查询还是慢&quot;的常见原因——慢的不是定位，是回表。</p><p><strong>覆盖索引</strong>正是为省掉第 2 步而存在：如果查询需要的所有列都已在索引里，就完全不必回表。有一个常被忘掉的推论：<strong>因为二级索引叶子自带主键值，所以 <code>SELECT 主键 FROM 表 WHERE 二级索引列 = ?</code> 本身就是覆盖索引</strong>，不需要额外建联合索引。</p><h3 id="主键选型-自增、uuid-与雪花算法的代价" tabindex="-1"><a class="header-anchor" href="#主键选型-自增、uuid-与雪花算法的代价"><span>主键选型：自增、UUID 与雪花算法的代价</span></a></h3><p>主键的代价不只在自己身上，<strong>它被复制进每一个二级索引</strong>。所以选主键时要同时算两笔账：插入时的页行为，以及所有二级索引的体积。</p><ul><li><strong>自增或趋势递增</strong>：插入落在 B+ 树最右侧，顺序追加。页写满才分裂，页利用率高，索引碎片少。</li><li><strong>纯随机（如 UUID v4）</strong>：每次插入落到树的随机位置，频繁触发页分裂并留下页内空洞，同样的数据占更多页，缓冲池命中率随之下降。同时 16 字节的随机值比 8 字节整数大一倍，还要乘以二级索引的数量。</li><li><strong>时间趋势递增（有序 UUID、雪花算法）</strong>：兼顾分布式唯一与顺序插入，是分布式场景的常见解。代价是它<strong>依赖时钟</strong>——时钟回拨会导致重复或乱序，机器标识的分配也需要额外机制。</li></ul><p>一个能直接用的判据：<strong>如果一张表有很多二级索引，主键宽度的代价会被放大若干倍</strong>。这也是&quot;主键宁短勿长&quot;这句话的真实来源。</p><h2 id="缓冲池-随机-i-o-是怎么被消掉的" tabindex="-1"><a class="header-anchor" href="#缓冲池-随机-i-o-是怎么被消掉的"><span>缓冲池：随机 I/O 是怎么被消掉的</span></a></h2><h3 id="缓冲池-页的状态与改进的-lru" tabindex="-1"><a class="header-anchor" href="#缓冲池-页的状态与改进的-lru"><span>缓冲池：页的状态与改进的 LRU</span></a></h3><p><strong>缓冲池</strong>是 InnoDB 的主内存区域，以页为单位缓存磁盘页，读写都先经过它。页在其中有三种状态：</p><ul><li><strong>空闲页</strong>：尚未被分配使用；</li><li><strong>干净页</strong>：内容与磁盘一致；</li><li><strong>脏页</strong>：已被修改，尚未写回磁盘。</li></ul><p>淘汰策略不是普通 LRU，而是<strong>改进的 LRU</strong>：链表中段作为插入点，新读入的页先进入&quot;老&quot;区，只有被再次访问才升到&quot;新&quot;区。这个设计的目的是<strong>让一次性的大范围扫描不要把整个缓冲池冲掉</strong>——全表扫描读进来的页只被用一次，会待在老区被优先淘汰，不会挤走热数据。</p><p>缓冲池的空间是有限的，而<strong>每个索引都要占一部分</strong>。这就是&quot;索引不是越多越好&quot;在内存层面的依据：多一个索引，就多一棵树要和数据页争夺缓冲池。</p><h3 id="change-buffer-非唯一二级索引的写为什么能缓" tabindex="-1"><a class="header-anchor" href="#change-buffer-非唯一二级索引的写为什么能缓"><span>Change Buffer：非唯一二级索引的写为什么能缓</span></a></h3><p>对二级索引的插入是<strong>随机顺序</strong>的，删除和更新还可能触及树上不相邻的多个二级索引页。如果每次写都要把对应页从磁盘读进来，随机 I/O 会非常可观。</p><p><strong>Change Buffer 的做法是把这些修改先缓在内存里</strong>，等那个页因为别的原因被读进来时再合并（merge），从而把多次随机写合并成一次读入。</p><p>它有一个必须记住的边界：<strong>只对非唯一的二级索引有效</strong>。原因是唯一索引在插入时必须判断唯一性，而判断唯一性就必须把页读进来——既然页已经读进来了，缓冲就没有意义。这是&quot;唯一索引写性能差&quot;的真正机制，也是选唯一索引时要一起考虑的代价。</p><h3 id="刷脏与双写缓冲-页什么时候写盘-写坏了怎么办" tabindex="-1"><a class="header-anchor" href="#刷脏与双写缓冲-页什么时候写盘-写坏了怎么办"><span>刷脏与双写缓冲：页什么时候写盘，写坏了怎么办</span></a></h3><p>脏页不能无限积压，否则崩溃后恢复要重放太多日志。推动刷脏的力量有两个：<strong>缓冲池脏页比例</strong>与 <strong>redo 日志的空间压力</strong>。后者常被忽略——redo 是环形写入的，如果脏页迟迟不刷，redo 会写满，此时数据库必须停下来强制刷脏，表现为周期性的性能抖动。</p><p>页在写盘过程中可能因为掉电只写了一部分，这种<strong>部分写</strong>会让一个页既不是旧内容也不是新内容。<strong>双写缓冲</strong>的处理办法是：刷盘前先把整页顺序写到一块专门的区域，再写目标位置；崩溃后如果目标页损坏，可以用这块区域里的完整副本恢复。它牺牲一次顺序写，换来页级的写入原子性。</p><h2 id="日志-为什么一次写入要记三份" tabindex="-1"><a class="header-anchor" href="#日志-为什么一次写入要记三份"><span>日志：为什么一次写入要记三份</span></a></h2><h3 id="wal-为什么必须先写日志再写数据页" tabindex="-1"><a class="header-anchor" href="#wal-为什么必须先写日志再写数据页"><span>WAL：为什么必须先写日志再写数据页</span></a></h3><p>随机写一页和顺序追加一条日志，成本相差很大。<strong>WAL（Write-Ahead Logging）</strong>就是利用这个差距：修改先在缓冲池里完成，同时把修改顺序写入日志并刷盘，数据页则由后台线程异步、批量写回。</p><p>于是事务提交时只需要保证<strong>日志落盘</strong>，而不需要等数据页落盘。持久性由日志保证，性能由延迟写数据页换来。</p><p>这里有一个必须知道的分档参数——<code>innodb_flush_log_at_trx_commit</code>，控制每次提交时日志刷到什么程度：</p><table><thead><tr><th>取值</th><th>事务提交时的行为</th><th>掉电会丢什么</th></tr></thead><tbody><tr><td>1</td><td>写入并刷盘</td><td>不丢已提交事务（默认，也是最安全的）</td></tr><tr><td>2</td><td>写入操作系统缓存，不刷盘</td><td>数据库进程崩溃不丢，<strong>操作系统崩溃或断电会丢</strong></td></tr><tr><td>0</td><td>交给后台每秒刷一次</td><td>进程崩溃就可能丢最近一秒</td></tr></tbody></table><p>把 1 改成 2 或 0 能显著提高写入吞吐，代价是持久性从&quot;事务级&quot;降级为&quot;秒级&quot;。这是一个必须由业务来回答的取舍，不是性能调优参数。</p><h3 id="redo-与-undo-崩溃恢复和回滚各要什么" tabindex="-1"><a class="header-anchor" href="#redo-与-undo-崩溃恢复和回滚各要什么"><span>redo 与 undo：崩溃恢复和回滚各要什么</span></a></h3><p>两者都叫日志，但解决的问题相反：</p><ul><li><strong>redo log</strong>：记录&quot;对这个页做了什么修改&quot;，用于<strong>崩溃后重做</strong>已提交的事务。它是物理性的页级修改记录，因此重放是幂等的——同一条 redo 重放多次结果一致，这正是崩溃恢复可以重复执行的前提。</li><li><strong>undo log</strong>：记录反向操作，用于<strong>事务回滚</strong>，同时为多版本并发控制提供旧版本数据。</li></ul><p>undo 的生命周期值得单独记：插入操作产生的 undo 在事务提交后即可回收；<strong>更新和删除产生的 undo 不能立刻回收</strong>，因为可能还有更早的读视图需要旧版本。这就是长事务的危害机制——它让 history list 无法收缩，undo 空间持续增长，并连带拖慢刷脏与 recovery。<strong>长事务的真实代价不在于锁，而在于它拖住了整个版本回收链。</strong></p><h3 id="binlog-与两阶段提交-两套日志为什么必须同时存在" tabindex="-1"><a class="header-anchor" href="#binlog-与两阶段提交-两套日志为什么必须同时存在"><span>binlog 与两阶段提交：两套日志为什么必须同时存在</span></a></h3><p>redo log 属于<strong>存储引擎层</strong>，binlog 属于<strong>服务器层</strong>，两者不能互相替代：</p><ul><li>redo 是物理的、幂等的，服务于<strong>崩溃恢复</strong>；</li><li>binlog 是逻辑的、按事务顺序记录的，服务于<strong>复制与时间点恢复</strong>。引擎对 binlog 一无所知，所以 binlog 无法用来恢复引擎内部的数据页。</li></ul><p>既然两套日志都要写，它们就必须一致，否则会出现&quot;主库认为事务已提交、从库收到的是另一次修改&quot;这类撕裂。<strong>两阶段提交</strong>解决这个问题：提交时先写 redo 并标记为 prepare，再写 binlog，最后把 redo 标记为 commit。崩溃恢复时的判定规则是：redo 处于 prepare 且对应的 binlog 完整，就提交；否则回滚。这个规则的实质是<strong>以 binlog 的完整性作为事务是否真正成立的裁决依据</strong>。</p><h2 id="一条-sql-的完整路径" tabindex="-1"><a class="header-anchor" href="#一条-sql-的完整路径"><span>一条 SQL 的完整路径</span></a></h2><h3 id="读路径-一条二级索引查询经过哪些结构" tabindex="-1"><a class="header-anchor" href="#读路径-一条二级索引查询经过哪些结构"><span>读路径：一条二级索引查询经过哪些结构</span></a></h3><p>以 <code>SELECT name FROM part WHERE part_no = &#39;P1001&#39;</code> 为例，先不考虑 <code>name</code> 是否在索引里：</p><ol><li>连接层接收语句，权限校验；</li><li>解析器做语法与语义分析，生成解析树；</li><li>优化器决定用哪个索引——这一步只看代价估算，不看数据；</li><li>执行器按计划驱动存储引擎；</li><li>InnoDB 在 <code>part_no</code> 的<strong>二级索引 B+ 树</strong>上从根走到叶，找到键值，取出主键值；</li><li>用主键回到<strong>聚簇索引 B+ 树</strong>取整行，这就是回表；</li><li>每一次树查找，如果目标页不在<strong>缓冲池</strong>，就从磁盘读入一页（16KB），并把淘汰出的脏页交给后台刷盘；</li><li>如果优化器判断接下来会顺序访问，会触发<strong>预读</strong>，把后续的页一起读进来。</li></ol><p>如果 <code>name</code> 也在索引里，第 6 步可以整步省掉。整条路径上<strong>唯一昂贵的动作是第 7 步的磁盘读</strong>，其他几乎都是内存操作——这解释了为什么优化索引的实质是&quot;减少页的访问次数&quot;，而不是&quot;减少 CPU 运算&quot;。</p><h3 id="写路径-一条-update-要改几处地方" tabindex="-1"><a class="header-anchor" href="#写路径-一条-update-要改几处地方"><span>写路径：一条 UPDATE 要改几处地方</span></a></h3><p>以 <code>UPDATE part SET name = &#39;X&#39; WHERE part_no = &#39;P1001&#39;</code> 为例：</p><ol><li>与读路径一样完成解析与优化，拿到要修改的行；</li><li>先把<strong>旧值写入 undo log</strong>，这是回滚与旧版本读的基础；</li><li>在<strong>缓冲池</strong>中直接修改该页，页被标记为<strong>脏页</strong>——注意，此时磁盘上的数据并没有变；</li><li>把修改按顺序写入 <strong>redo log</strong>，并写入 <strong>binlog</strong>，以两阶段提交的次序完成；</li><li>事务提交，按配置的刷盘档位保证 redo 落盘；</li><li>之后由后台线程<strong>异步批量</strong>把脏页写回磁盘，写之前经过<strong>双写缓冲</strong>防止部分写；</li><li>一个 UPDATE 若改了二级索引列，还要额外维护相应的二级索引页，这些页可能不在缓冲池里——此时 <strong>Change Buffer</strong> 能把随机写先缓起来（前提是索引非唯一）。</li></ol><p>把 5 和 6 分开看是关键：<strong>事务提交的代价与数据落盘的代价被解耦了</strong>。这也是为什么&quot;提交成功&quot;不等于&quot;数据已在磁盘上的最终位置&quot;——它只意味着日志已经足够重放这次修改。</p><h2 id="索引的边界-什么时候用不上、什么时候不该建" tabindex="-1"><a class="header-anchor" href="#索引的边界-什么时候用不上、什么时候不该建"><span>索引的边界：什么时候用不上、什么时候不该建</span></a></h2><h3 id="最左前缀与范围截断-联合索引什么时候只用得上前半段" tabindex="-1"><a class="header-anchor" href="#最左前缀与范围截断-联合索引什么时候只用得上前半段"><span>最左前缀与范围截断：联合索引什么时候只用得上前半段</span></a></h3><p>联合索引 <code>(a, b, c)</code> 的键是<strong>按 a、再按 b、再按 c 排序</strong>的。查找时只能从最左边开始连续匹配：</p><ul><li><code>WHERE a = ?</code>：用得上 a；</li><li><code>WHERE a = ? AND b = ?</code>：用得上 a、b；</li><li><code>WHERE b = ?</code>：<strong>用不上</strong>——因为索引先按 a 排序，b 的局部有序性无法在缺少 a 的情况下利用（8.0.13 之后有一种例外：当 a 的取值种类很少时优化器可能做跳过扫描，但那是依赖统计信息的退化用法，不能当作常规设计依据）；</li><li><code>WHERE a = ? AND b &gt; ? AND c = ?</code>：a 用于定位，b 用于范围，<strong>c 无法再用于定位与排序</strong>。</li></ul><p>最后一条是<strong>范围截断</strong>，也是最常被记错的一条：范围条件之后的列并非&quot;索引失效&quot;，而是从&quot;定位列&quot;降级为&quot;过滤条件&quot;。它仍然可以在存储引擎层被用来过滤（减少回表），但不会缩小索引的扫描范围，<code>key_len</code> 也不会包含它。<strong><code>&gt;</code>、<code>&lt;</code>、<code>&gt;=</code>、<code>&lt;=</code>、<code>BETWEEN</code>、<code>LIKE &#39;x%&#39;</code> 都属于范围条件，没有例外</strong>——把 <code>&gt;=</code> 当作能规避截断的写法是流传很广的误解。</p><p>同理，<code>ORDER BY</code> 是否能用上索引的排序能力，取决于排序列是不是索引的连续前缀。一旦中间夹了范围条件，后面的有序性就用不上了。</p><h3 id="索引列上的运算-函数、隐式转换与前缀匹配" tabindex="-1"><a class="header-anchor" href="#索引列上的运算-函数、隐式转换与前缀匹配"><span>索引列上的运算：函数、隐式转换与前缀匹配</span></a></h3><p>索引树是按<strong>列的原始值</strong>排序的。一旦查询条件里对这个值做了变换，树上的顺序就无法对应：</p><ul><li><strong>对列使用函数或表达式</strong>：<code>WHERE DATE(create_time) = &#39;2024-01-01&#39;</code>、<code>WHERE id + 1 = 5</code>，都让索引无法用于定位。前者的正确写法是把变换挪到常量一侧（改成范围条件），后者的正确写法是解出 <code>id</code>。</li><li><strong>隐式类型转换</strong>：<code>phone</code> 是 <code>varchar</code>，写成 <code>WHERE phone = 13800138000</code> 时，MySQL 会把<strong>列</strong>转换成数字再比较，索引因此失效。字符串列必须用引号。</li><li><strong>字符集或排序规则不一致的连接</strong>：两个列做连接时若字符集不同，比较前会发生转换，同样用不上索引。</li><li><strong>前缀匹配</strong>：<code>LIKE &#39;abc%&#39;</code> 可以用索引；<code>LIKE &#39;%abc&#39;</code> 不可以，因为树从左往右排序，右边的部分无法定位。这类需求要靠全文索引或搜索引擎解决。</li><li><strong>长字符串列</strong>：可以只对前 n 个字符建索引。代价是它<strong>不能被覆盖</strong>——因为索引里只有前缀，取完整值必须回表比对。前缀长度的选择靠区分度实测，而不是拍一个数。</li></ul><h3 id="区分度与优化器-索引存在不等于会被使用" tabindex="-1"><a class="header-anchor" href="#区分度与优化器-索引存在不等于会被使用"><span>区分度与优化器：索引存在不等于会被使用</span></a></h3><p>优化器选择索引是<strong>按代价</strong>的，不是按&quot;有没有&quot;。它会估算两条路的成本：走索引（索引树查找 + 逐行回表，回表是随机 I/O）与全表扫描（顺序读完整张表，一次读一页可拿多行）。</p><p>当条件命中的行占比不小的时候，全表扫描的顺序 I/O 反而更便宜，优化器会<strong>主动放弃索引</strong>。所以&quot;这条 SQL 没走索引&quot;要先分清两种性质完全不同的情况：</p><ul><li><strong>用不上的</strong>：上面那些让树序失效的写法，是设计或写法问题；</li><li><strong>不划算的</strong>：区分度太低（例如只有两三个取值的状态列）、命中比例太高，优化器算出来全表扫描更便宜。这是<strong>正确决策</strong>，把索引&quot;强制&quot;用上通常更慢。</li></ul><p>判断依据是区分度：<code>count(distinct 列) / count(*)</code>。越接近 1 越适合建索引；取值只有个位数的列，建了多半也不会被用，只是白付维护成本。</p><h3 id="null-与索引-唯一索引为什么不唯一" tabindex="-1"><a class="header-anchor" href="#null-与索引-唯一索引为什么不唯一"><span>NULL 与索引：唯一索引为什么不唯一</span></a></h3><p>前面提到允许为 NULL 的列只占 1 bit，不占数据空间。但 NULL 在索引与约束上的行为会反过来咬人。</p><p><strong>索引项对 NULL 是建立的</strong>，所以 <code>IS NULL</code> 本身是可以走索引的。至于 <code>IS NOT NULL</code>——它同样<strong>可以</strong>走索引，只是优化器经常放弃它，因为&quot;非空&quot;往往命中大部分行，按代价算不如全表扫描。所以&quot;<code>IS NULL</code> 走索引、<code>IS NOT NULL</code> 不走索引&quot;是个过度简化的说法，正确的表述是<strong>两者都具备走索引的能力，是否真的走由选择度决定</strong>。</p><p>真正需要单独记住的是这条：<strong>唯一索引允许多个 NULL</strong>。NULL 在 SQL 里表示&quot;未知&quot;，而&quot;未知&quot;不等于&quot;未知&quot;，所以唯一性检查不会把两个 NULL 视为冲突。后果是：</p><div class="language-sql line-numbers-mode" data-highlighter="shiki" data-ext="sql" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-sql"><span class="line"><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">CREATE</span><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;"> TABLE</span><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;"> part</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;"> (</span></span>
<span class="line"><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">  part_no  </span><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">VARCHAR</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">(</span><span style="--shiki-light:#986801;--shiki-dark:#D19A66;">32</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">) </span><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">NOT NULL</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">,</span></span>
<span class="line"><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">  model_no </span><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">VARCHAR</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">(</span><span style="--shiki-light:#986801;--shiki-dark:#D19A66;">32</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">) </span><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">NULL</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">,          </span><span style="--shiki-light:#A0A1A7;--shiki-light-font-style:italic;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;">-- 本想保证&quot;型号不重复&quot;</span></span>
<span class="line"><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">  UNIQUE</span><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;"> KEY</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;"> uk_part_model (model_no)</span></span>
<span class="line"><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#A0A1A7;--shiki-light-font-style:italic;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;">-- 下面这些插入全部成功，唯一索引一条都没拦住</span></span>
<span class="line"><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">INSERT INTO</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;"> part </span><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">VALUES</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;"> (</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;">&#39;P1&#39;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">, </span><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">NULL</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">);</span></span>
<span class="line"><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">INSERT INTO</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;"> part </span><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">VALUES</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;"> (</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;">&#39;P2&#39;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">, </span><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">NULL</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">);</span></span>
<span class="line"><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">INSERT INTO</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;"> part </span><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">VALUES</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;"> (</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;">&#39;P3&#39;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">, </span><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">NULL</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">);</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>只要这一列可为空，<strong>唯一索引就不保证业务唯一</strong>：绕过它只需要插 NULL。所以&quot;业务唯一&quot;的落法是<strong>唯一索引 + <code>NOT NULL</code></strong>两件事一起做，缺一不可。</p><p><code>NOT NULL</code> 因此不只是数据质量的偏好，它还有第二个作用：<strong>让优化器确切知道这一列不会有 NULL</strong>，从而在做索引选择与统计时少一层不确定性。所以&quot;业务唯一&quot;和&quot;取值必填&quot;这两件事，最好在同一个字段定义里一次说清。</p><p>最后顺带解开一个高频疑问：<code>count(*)</code> 与 <code>count(列)</code> 结果不同不是 bug——后者只统计该列<strong>非 NULL</strong> 的行。另外 InnoDB 不存储表的总行数，<code>count(*)</code> 必须实际扫描，它会选择<strong>体积最小的二级索引</strong>来扫，以减少要读的页数；这就是为什么 <code>count(*)</code> 通常比 <code>count(主键)</code> 和 <code>count(普通列)</code> 更快。</p><h3 id="什么时候不该建索引" tabindex="-1"><a class="header-anchor" href="#什么时候不该建索引"><span>什么时候不该建索引</span></a></h3><p>上面讲的是&quot;索引为什么用不上&quot;，还有一类问题反过来：<strong>索引建了、也用了，但整体是负收益</strong>。</p><ul><li><strong>区分度极低</strong>：优化器多半不会用，只剩维护成本。</li><li><strong>写多读少</strong>：每一次插入、更新、删除都要同步维护所有索引。写入密集的表上，索引数量直接决定写入延迟。</li><li><strong>表本身很小</strong>：全表扫描只需要一两次 I/O，索引没有优化空间。</li><li><strong>该列频繁被更新</strong>：更新索引列往往等价于&quot;删一条索引项再插一条&quot;，代价高于更新普通列。</li><li><strong>长字符串列直接建全列索引</strong>：既占空间又拖慢写入，应该考虑前缀索引或改造查询。</li><li><strong>查询普遍是 <code>SELECT *</code></strong>：覆盖索引永远无法生效，每次都要回表，索引的收益被大幅压缩。</li></ul><p>还有一条容易被忽略：<strong>索引占缓冲池</strong>。多一棵树就和数据页争夺内存，可能让原本命中的热数据被挤出。所以&quot;加索引&quot;不是无成本的优化动作，它同时在写入路径、存储空间和内存三处收费。</p>`,97)])}var p=c(u,[[`render`,f]]);export{l as _pageData,p as default};