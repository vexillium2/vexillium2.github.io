import{a as e,i as t,u as n}from"./app-Cq4apPnC.js";import{t as r}from"./plugin-vue_export-helper-BDNMzG2s.js";var i=JSON.parse(`{"path":"/zh/posts/notes/ai-swe/06-harness.html","title":"Agent Harness 工程","lang":"zh-CH","frontmatter":{"lang":"zh-CH","title":"Agent Harness 工程","description":"以 Claude Code / Codex / OpenHands 为样本的沙箱执行、权限、会话状态与上下文管理工程","date":"2026-09-07T00:00:00.000Z","category":["后端开发"],"tag":["人工智能"],"head":[["script",{"type":"application/ld+json"},"{\\"@context\\":\\"https://schema.org\\",\\"@type\\":\\"Article\\",\\"headline\\":\\"Agent Harness 工程\\",\\"image\\":[\\"\\"],\\"datePublished\\":\\"2026-09-07T00:00:00.000Z\\",\\"dateModified\\":\\"2026-09-09T17:20:47.000Z\\",\\"author\\":[{\\"@type\\":\\"Person\\",\\"name\\":\\"vexillium\\",\\"url\\":\\"https://vexillium2.github.io\\"}]}"],["meta",{"property":"og:url","content":"https://vexillium2.github.io/zh/posts/notes/ai-swe/06-harness.html"}],["meta",{"property":"og:site_name","content":"Vexilog"}],["meta",{"property":"og:title","content":"Agent Harness 工程"}],["meta",{"property":"og:description","content":"以 Claude Code / Codex / OpenHands 为样本的沙箱执行、权限、会话状态与上下文管理工程"}],["meta",{"property":"og:type","content":"article"}],["meta",{"property":"og:locale","content":"zh-CH"}],["meta",{"property":"og:locale:alternate","content":"zh-CN"}],["meta",{"property":"og:updated_time","content":"2026-09-09T17:20:47.000Z"}],["meta",{"property":"article:tag","content":"人工智能"}],["meta",{"property":"article:published_time","content":"2026-09-07T00:00:00.000Z"}],["meta",{"property":"article:modified_time","content":"2026-09-09T17:20:47.000Z"}],["link",{"rel":"alternate","hreflang":"zh-cn","href":"https://vexillium2.github.io/zh/posts/notes/ai-swe/06-harness.html"}]]},"git":{"createdTime":1788762188000,"updatedTime":1788974447000,"contributors":[{"name":"vexillium2","username":"vexillium2","email":"ntc_ip_rbm@163.com","commits":3,"url":"https://github.com/vexillium2"}]},"readingTime":{"minutes":25.46,"words":7639},"filePathRelative":"zh/posts/notes/ai-swe/06-harness.md","excerpt":"<p>这一章讨论模型循环外面那一圈工程外壳——<strong>Agent Harness</strong>。模型只负责&quot;下一步做什么&quot;的决策，可决策产生之前与之后的所有事情，都需要 harness 兜住：动作在哪执行、越权怎么办、跑到一半断电怎么恢复、上下文怎么在不失真的前提下省着用、烧钱失控了谁来踩刹车、被恶意内容诱导时最后一道闸在哪、出问题后凭什么复盘。编码类 Agent（如 Claude Code、Codex、OpenHands）是观察 harness 结构最好的样本——它们把同样的循环包装进沙箱、权限与状态体系里，差异只在取舍。凡是会随版本变化的厂商细节，一律按&quot;该看什么维度、如何自测&quot;处理，不写死结论。</p>"}`),a={name:`06-harness.md`};function o(r,i,a,o,s,c){return n(),t(`div`,null,[...i[0]||=[e(`<p>这一章讨论模型循环外面那一圈工程外壳——<strong>Agent Harness</strong>。模型只负责&quot;下一步做什么&quot;的决策，可决策产生之前与之后的所有事情，都需要 harness 兜住：动作在哪执行、越权怎么办、跑到一半断电怎么恢复、上下文怎么在不失真的前提下省着用、烧钱失控了谁来踩刹车、被恶意内容诱导时最后一道闸在哪、出问题后凭什么复盘。编码类 Agent（如 Claude Code、Codex、OpenHands）是观察 harness 结构最好的样本——它们把同样的循环包装进沙箱、权限与状态体系里，差异只在取舍。凡是会随版本变化的厂商细节，一律按&quot;该看什么维度、如何自测&quot;处理，不写死结论。</p><h2 id="agent-harness-是什么-模型之外的那台-机器" tabindex="-1"><a class="header-anchor" href="#agent-harness-是什么-模型之外的那台-机器"><span>Agent Harness 是什么：模型之外的那台&quot;机器&quot;</span></a></h2><p>Agent 的决策内核（LLM）、行动接口（工具）与循环控制构成了运行时；但光有循环，一个 Agent 还不能上生产。<strong>Harness 是把&quot;会决策的循环&quot;变成&quot;可被约束、可恢复、可审计、可预算化的产品&quot;的全部工程外壳</strong>。判断一件事是否属于 harness 的职责有一条简单的判据：<strong>把模型拿掉，这件事是否依然必须存在</strong>——沙箱必须存在（动作要执行），权限必须存在（越权要拦截），状态必须存在（中断要恢复），预算与观测必须存在。凡是&quot;模型没了就不需要&quot;的东西（如何规划、如何反思），不属于 harness。</p><p>以编码类 Agent 为样本，可以拆出 harness 的六块骨架，它们之间是串行拦截而非并列关系：</p><ul><li><strong>执行环境</strong>：模型声明的动作（shell 命令、文件编辑、网络请求）落在哪里——本机裸跑还是隔离沙箱，读写权限边界怎么划。</li><li><strong>权限裁决</strong>：每次动作执行前，按&quot;动作名 + 参数&quot;判定放行、拒绝还是请示，裁决独立于模型输出，模型无法绕开。</li><li><strong>状态与恢复</strong>：把对话上下文、运行中计划、已产生副作用的工作区落盘，支持中断续跑与多会话隔离。</li><li><strong>上下文管理</strong>：在窗口有限的前提下决定什么进上下文、什么外置、何时压缩——这是 harness 对模型最直接的&quot;照料&quot;。</li><li><strong>资源治理</strong>：给每一步决策设定 token、时间与成本上限，保证失控时可被硬性终止。</li><li><strong>可观测与安全</strong>：记录每一步决策与动作的痕迹，既用于排障复盘，也是安全审计的依据。</li></ul><p>一个典型的编码 Agent 运行时，模型输出&quot;读文件&quot;或&quot;跑测试&quot;的结构化意图后，harness 依次做四件事：先过权限裁决（允许/拒绝/问人），再在沙箱内执行，把结果连同这次动作的审计记录写进会话状态，随后做上下文管理（这段结果要不要压缩）。这四道工序每一步都可能让一次&quot;模型很自信的调用&quot;被拦下或改写——<strong>harness 的本质是给模型的自由上边界，边界内模型说了算，边界外模型碰不到</strong>。</p><p>工程上最常见的方向性错误，是把 harness 环节当成&quot;可后补的增强&quot;：先把模型循环跑通、再把权限和状态随手一贴。但沙箱、权限、状态、压缩这些机制是耦合设计的——沙箱决定权限能在哪一层生效，权限决定恢复时能放行什么，压缩决定状态里要留哪些原始信息。<strong>harness 必须从第一天就按产品约束设计，而不是模型能跑之后再打补丁</strong>；否则等到要上生产时会发现，安全与恢复能力不是&quot;加一个模块&quot;能补上的，而是需要重构执行路径本身。</p><h2 id="沙箱执行-动作发生在哪-决定了风险有多大" tabindex="-1"><a class="header-anchor" href="#沙箱执行-动作发生在哪-决定了风险有多大"><span>沙箱执行：动作发生在哪，决定了风险有多大</span></a></h2><h3 id="为什么需要隔离" tabindex="-1"><a class="header-anchor" href="#为什么需要隔离"><span>为什么需要隔离</span></a></h3><p>Agent 的动作有两类来源，风险差异极大：一类是<strong>结构性工具</strong>（读文件、查库、调 API），动作边界由工具代码写死；另一类是<strong>自由执行</strong>（跑 shell、执行生成的代码、安装依赖），模型输出什么就执行什么，动作边界几乎不存在。自由执行是 Agent 能力强的来源，也是事故的来源——模型可能因推理错误、上下文被污染或目标漂移，执行一条它自己都意识不到后果的命令。<strong>隔离的目标不是&quot;防止模型作恶&quot;，而是&quot;限制模型出错时能造成的最大损失&quot;</strong>。隔离粒度决定一件事：一次越界动作，毁掉的是当前工作目录、整个容器、还是宿主机。</p><h3 id="隔离技术的取舍-容器-微虚拟机-wasm" tabindex="-1"><a class="header-anchor" href="#隔离技术的取舍-容器-微虚拟机-wasm"><span>隔离技术的取舍：容器 / 微虚拟机 / WASM</span></a></h3><table><thead><tr><th>方案</th><th>隔离边界</th><th>启动与性能开销</th><th>兼容性</th><th>典型角色</th></tr></thead><tbody><tr><td>容器（如 Docker）</td><td>内核共享，靠 namespaces/cgroups 隔离</td><td>极低，毫秒级</td><td>高，近似原生</td><td>默认选择，隔离的是&quot;路径&quot;而非&quot;内核&quot;</td></tr><tr><td>用户态内核（如 gVisor）</td><td>系统调用被用户态拦截、改写</td><td>中，syscall 密集场景明显</td><td>较高</td><td>需要比裸容器更强的边界又不愿付虚机成本</td></tr><tr><td>微虚拟机（如 Firecracker）</td><td>硬件虚拟化，独立内核</td><td>较高，启动百毫秒级</td><td>中，内核/驱动差异</td><td>多租户、不可信代码、强隔离诉求</td></tr><tr><td>WASM 沙箱</td><td>语言/运行时级，无系统调用面</td><td>低</td><td>低，只能跑编译到 WASM 的程序</td><td>插件体系、内存安全优先的场景</td></tr></tbody></table><p>选型的关键不是&quot;哪个更安全&quot;，而是<strong>你的威胁模型里，对手与受害者的距离有多近</strong>：同一个人自己的开发机、只防&quot;模型手滑删库&quot;，一层容器 + 文件权限就够；要承接不可信代码或多租户隔离，容器共享内核这个事实本身就不可接受，必须上到内核隔离。反过来，为所有任务一律套微虚拟机，启动开销与镜像维护成本会抵消 Agent 快速试错的收益。<strong>隔离强度应当与&quot;动作能访问的资产价值&quot;匹配，而不是统一拉满</strong>——跑测试用的沙箱与跑生产部署的沙箱不应是同一个配置。</p><h3 id="文件系统与网络的策略-才是沙箱的真正边界" tabindex="-1"><a class="header-anchor" href="#文件系统与网络的策略-才是沙箱的真正边界"><span>文件系统与网络的策略，才是沙箱的真正边界</span></a></h3><p>技术选型只决定&quot;隔离壳有多厚&quot;，真正决定事故半径的是<strong>壳内可见的资产</strong>：</p><ul><li><strong>文件系统默认只读，按需挂载可写目录</strong>。模型唯一能写坏的是显式交给它的工作区，其余路径（系统目录、其他项目、用户主目录）只读或不可见。把整个主目录可写地挂进去，等于没隔离。</li><li><strong>网络默认关闭或出向白名单</strong>。很多高危事故的链路是&quot;模型读到了密钥 → 发出网络请求外带&quot;，网络层是最后一道拦截。本地开发全放开、生产执行收紧是常见分级，但没有&quot;外发即审计&quot;的地方，文件泄露出去你都无从知道。</li><li><strong>临时环境一用即弃</strong>。每次任务用全新沙箱，任务结束销毁；复用&quot;上次的沙箱&quot;会把上次的残留文件变成这次的攻击面或错误源。</li><li><strong>时间与资源同样属于隔离维度</strong>。无限制的 CPU/内存配额会让一次失控循环拖垮宿主机——这属于资源治理的职责，但配额要在沙箱层声明。</li></ul><p>结构性工具（读文件、编辑文件）也应在沙箱内以受限身份执行，而不是在宿主机上以进程所有者权限直跑——否则沙箱只挡住了 shell，没挡住同样危险的工具调用。<strong>隔离的是&quot;执行者身份&quot;而非&quot;调用方式&quot;</strong>，这个认知比具体技术选型更重要。</p><h2 id="权限系统-把-默认拒绝-变成可执行的裁决" tabindex="-1"><a class="header-anchor" href="#权限系统-把-默认拒绝-变成可执行的裁决"><span>权限系统：把&quot;默认拒绝&quot;变成可执行的裁决</span></a></h2><p>沙箱管住&quot;能碰什么&quot;，权限管住&quot;能做什么&quot;。编码类 Agent 的权限系统几乎都是同一套裁决模型，值得拆开看：</p><ul><li><strong>动作命名空间与白名单</strong>。Agent 可调用的不是&quot;任意 shell 命令&quot;，而是一个受限命令集合（文件读、文件写、执行测试、跑构建……）。白名单在 harness 层声明，模型只能看到白名单内的工具，看不到的连&quot;尝试&quot;都无从发起。这比&quot;事后发现越权再拦截&quot;安全一个量级——<strong>不存在于可见范围内的动作，天然不可能被调用</strong>。</li><li><strong>参数级裁决，而非命令级</strong>。命令名允许不等于命令安全：<code>rm</code> 是合法工具，<code>rm -rf /</code> 与 <code>rm -rf ./dist</code> 风险天差地别。裁决必须落到参数：删除命令作用在项目目录内才放行，越出边界进入询问或拒绝。只看命令名放行、不校验参数，等于把&quot;危险&quot;的判断外包给了模型。</li><li><strong>分级策略：自动放行 / 询问 / 拒绝</strong>。低危高频动作（读文件、只读查询）按规则自动放行；中危（写文件、跑测试）按路径与内容匹配自动放行或弹确认；高危（删库、外发网络、安装全局依赖）无论参数如何都进人工确认或直接拒绝。分层是为了把人工注意力留给真正需要判断的动作——<strong>确认框本身是稀缺资源，滥用会让用户闭眼点允许</strong>。</li><li><strong>最小权限的落地形态</strong>。进程以最小必要身份运行：不 root、不带凭据、只有当前任务需要的目录可写。session 结束时身份与凭据随之销毁，不让权限残留到下一个任务。</li></ul><h3 id="权限系统设计的反例与陷阱" tabindex="-1"><a class="header-anchor" href="#权限系统设计的反例与陷阱"><span>权限系统设计的反例与陷阱</span></a></h3><ul><li><strong>把权限写进 prompt 而不是裁决器</strong>。在系统提示里写&quot;不要删除生产库&quot;毫无约束力——模型可能因注入或幻觉输出删除命令，而提示只是一段文本。<strong>权限是运行时行为，不是模型约定</strong>。凡安全要求必须落到裁决代码。</li><li><strong>白名单只圈命令、不圈来源</strong>。命令白名单 + 允许 Agent 执行任意脚本内容（<code>bash &lt;(curl ...)</code>、<code>eval</code>）会把&quot;你审过的命令&quot;绕过成&quot;模型现场编的命令&quot;。要禁的是&quot;执行未经过滤的输入&quot;，而非某一串命令。</li><li><strong>全自动放行 + 无审计</strong>。为了不打断 Agent 把每件事都自动放行，等于放弃了权限层全部意义，只剩沙箱兜底。即使自动放行，也必须给每次放行留审计轨迹——否则出了事故，权限层无法回答&quot;那次危险命令是谁批的&quot;。</li><li><strong>权限与沙箱分离配置</strong>。沙箱内可写目录和权限系统允许写的目录不一致时，会出现&quot;裁决允许了、沙箱没给路径&quot;或&quot;沙箱给了、裁决没审&quot;的裂缝。两者应共享同一份路径白名单定义。</li></ul><p>权限系统与&quot;人工在环&quot;的关系是：<strong>机器能可靠裁决的交给规则，规则判不了的交给人，但人只做二选一的判断题</strong>（放行/拒绝 + 是否记住），不做填空题。</p><h2 id="会话与状态管理-checkpoint、断点续跑与多会话隔离" tabindex="-1"><a class="header-anchor" href="#会话与状态管理-checkpoint、断点续跑与多会话隔离"><span>会话与状态管理：checkpoint、断点续跑与多会话隔离</span></a></h2><h3 id="状态到底包含什么" tabindex="-1"><a class="header-anchor" href="#状态到底包含什么"><span>状态到底包含什么</span></a></h3><p>一次 Agent 会话产生的状态远比&quot;聊天记录&quot;多。至少分三层：</p><ul><li><strong>对话上下文</strong>：消息序列（含工具调用与结果），模型继续推理的依据。</li><li><strong>运行态对象</strong>：当前计划、待办列表、已用步数与预算、目标定义。这些不一定都进上下文，但必须被持久化，否则恢复后模型不知道自己进行到哪。</li><li><strong>工作区副作用</strong>：沙箱里被创建/修改的文件、安装的依赖、启动的进程。这是最容易漏的一层——<strong>只恢复对话、不恢复文件，Agent 续跑时面对的是一个&quot;失忆的世界&quot;</strong>。</li></ul><p>checkpoint 要同时覆盖这三层才算完整。编码类 Agent 普遍采用&quot;每个工具动作边界落一次 checkpoint&quot;的策略：动作前记录状态、动作后确认落盘，这样任意一步失败都能回退到上一个一致性点。</p><h3 id="断点续跑的正确姿势与难点" tabindex="-1"><a class="header-anchor" href="#断点续跑的正确姿势与难点"><span>断点续跑的正确姿势与难点</span></a></h3><p>续跑不是&quot;把消息塞回去重新问一次&quot;。正确的恢复顺序是：恢复工作区（文件回到 checkpoint 状态）→ 恢复运行态对象 → 恢复对话上下文 → 提示模型&quot;你正在继续之前的任务&quot;。其中最大的工程难点是<strong>副作用的一致性</strong>：</p><ul><li>文件类副作用可以通过快照/补丁栈回滚，相对可控；</li><li><strong>外部副作用不可回滚</strong>：已经发出的网络请求、已经写入远端数据库的数据、已经发送的消息，checkpoint 恢复不了。对这类动作，harness 只能靠&quot;幂等设计 + 恢复时明确告知模型哪些动作已实际生效&quot;来兜底，而不是假装时间倒流。</li><li>恢复后要给模型一个明确信号：哪些步骤已完成、哪些结果不可信需重验。模型自己从消息历史推断，往往会重复执行已完成的外部副作用。</li></ul><h3 id="多会话隔离-并行与冲突" tabindex="-1"><a class="header-anchor" href="#多会话隔离-并行与冲突"><span>多会话隔离：并行与冲突</span></a></h3><p>一个 harness 上可以同时跑多个会话：同一任务多开对比、不同任务并行。隔离的两个层次：</p><ul><li><strong>状态隔离</strong>：会话 A 的上下文、计划、checkpoint 对会话 B 不可见。这个靠&quot;状态按 session id 分桶&quot;实现，模型是共享的还是独立的都无所谓——状态边界必须清晰。</li><li><strong>工作区隔离</strong>：两个会话改同一份文件，冲突与互相覆盖不可避免。要么每会话独立工作副本（成本高但安全），要么共享工作区 + 变更锁。现实产品多采用&quot;工作区 + 变更应用前先 diff 确认&quot;的策略，让冲突暴露在裁决层而不是静默覆盖。</li></ul><h3 id="反例与常见失败" tabindex="-1"><a class="header-anchor" href="#反例与常见失败"><span>反例与常见失败</span></a></h3><ul><li><strong>只存对话不存运行态</strong>：恢复后模型有上下文却丢了目标与计划，续跑变成&quot;重新开始&quot;。</li><li><strong>checkpoint 频率过低</strong>：每 10 步存一次，第 9 步崩溃就丢 9 步的推理与副作用。步级 checkpoint 是成本与安全的默认平衡点。</li><li><strong>恢复时把外部副作用当没发生</strong>：以为恢复到了过去，实际外部世界已经变了——恢复的只是&quot;你的状态&quot;，不是&quot;世界的状态&quot;。</li><li><strong>会话之间共享工作区却无锁</strong>：并发 Agent 互相覆盖文件，产出的代码无法合并，排查时归因到&quot;另一个会话改了它&quot;。</li></ul><h2 id="上下文管理工程-让上下文-长跑不胖、坏了能修" tabindex="-1"><a class="header-anchor" href="#上下文管理工程-让上下文-长跑不胖、坏了能修"><span>上下文管理工程：让上下文&quot;长跑不胖、坏了能修&quot;</span></a></h2><p>上下文窗口是 Agent 最稀缺的资源。组装&quot;什么进窗口&quot;是运行时的话题，harness 要解决的是<strong>窗口装不下的工程机制</strong>：窗口满了怎么办、大内容怎么喂、哪些内容根本不该进窗口。</p><h3 id="compaction-与-summarization-窗口满了怎么办" tabindex="-1"><a class="header-anchor" href="#compaction-与-summarization-窗口满了怎么办"><span>Compaction 与 Summarization：窗口满了怎么办</span></a></h3><p>长会话必然逼近窗口上限。粗暴做法是<strong>截断</strong>：丢掉最旧的消息。它的致命缺陷是丢的是&quot;最早的目标设定与用户约束&quot;，而这恰恰是最不能丢的——模型截断后会在没有目标的情况下继续执行，目标漂移由此而来。</p><p>harness 采用的主流机制是 <strong>compaction（压缩）</strong>：把已经执行过的、不再需要逐字保留的旧内容压缩成摘要，腾出空间。它与截断的区别在于<strong>摘要保留了信息语义</strong>。工程上的三个关键决策：</p><ul><li><strong>触发时机</strong>：到达 token 阈值自动触发、每 N 轮主动触发、还是模型或用户手动触发？纯自动压缩的坑是&quot;压缩发生在推理中途&quot;，摘要本身会引入偏差。多数成熟产品支持手动 + 阈值结合。</li><li><strong>哪些内容可压缩、哪些必须保真</strong>：用户原始指令、系统级约束、当前任务目标必须原样保留；工具往返的历史可以摘要化。<strong>摘要模型自己做的判断不可全信</strong>——压缩后的内容会作为后续决策依据，摘要错了，后续全错。因此压缩结果要尽量由独立于主决策的一次调用产生，并保留&quot;压缩发生在第几步、原始内容存哪&quot;的索引，供需要时回查。</li><li><strong>压缩不是删除，是移动</strong>：原始完整轨迹应落盘（进 trace/审计存储），上下文里只留摘要。这样调试时仍能拿到原始证据，只是不在模型窗口里。</li></ul><h3 id="大文件读取-别把整个代码库塞进窗口" tabindex="-1"><a class="header-anchor" href="#大文件读取-别把整个代码库塞进窗口"><span>大文件读取：别把整个代码库塞进窗口</span></a></h3><p>Agent 处理代码任务时最大的上下文黑洞是&quot;读文件&quot;。三个工程原则：</p><ul><li><strong>先定位后读取，能不全量就不全量</strong>：先用搜索/索引确定相关符号与行号，再按需读片段；而不是把整个文件甚至整个项目 cat 进上下文。</li><li><strong>分块读取 + 行号续读</strong>：超出单次可读上限的文件分段读，每次带行号范围，让模型知道&quot;我看的是 100-200 行，前面还有&quot;。没有行号与边界的分块，模型会误以为读到的是全文。</li><li><strong>read 工具与 shell 的差异</strong>：文件读取走专用工具（返回结构化内容、受大小限制与权限控制），而不是让模型自由 shell cat。shell cat 无大小护栏、无权限裁决、无审计——大文件读取策略首先要在工具层面堵死这条自由路。</li></ul><h3 id="todo-列表外置-什么不该进上下文" tabindex="-1"><a class="header-anchor" href="#todo-列表外置-什么不该进上下文"><span>Todo 列表外置：什么不该进上下文</span></a></h3><p>Agent 的执行计划、待办清单这类<strong>运行态</strong>，如果放在上下文里让模型&quot;记着&quot;，会占窗口且在压缩时被改写。harness 的做法是把它们<strong>外置成文件</strong>，通过文件工具读写。好处是：不占上下文、压缩不丢失、断点续跑时直接恢复、还能被其他进程（比如 UI 展示、审计）读取。模型需要时读当前 todo，更新后写回。注意这里的坑：<strong>外置 todo 与模型&quot;口述&quot;的进度可能不一致</strong>——模型说&quot;做完了&quot;但 todo 文件没更新，要以文件为准做一致性校验，而不是信模型口头汇报。</p><h2 id="资源治理-给不确定的循环装上保险丝" tabindex="-1"><a class="header-anchor" href="#资源治理-给不确定的循环装上保险丝"><span>资源治理：给不确定的循环装上保险丝</span></a></h2><p>Agent 是不确定系统：同一个任务，可能 5 步完成也可能 50 步绕圈；可能一次调用就够，也可能在同一个错误上反复重试。<strong>没有资源上限的 Agent 循环，成本与耗时都是无界的</strong>。harness 的资源治理就是给这个不确定系统设定各种上限，并在超限时强制执行——注意&quot;强制执行&quot;，写在提示词里的软预算（&quot;请尽量少用几步&quot;）模型会无视。</p><h3 id="几个必须有的维度" tabindex="-1"><a class="header-anchor" href="#几个必须有的维度"><span>几个必须有的维度</span></a></h3><ul><li><strong>单次调用上限</strong>：一次 LLM 调用的最大输出 token、最大耗时。超时后重试或降级，避免一次卡死的调用挂住整个任务。</li><li><strong>单步执行上限</strong>：一次工具调用的超时与重试次数。工具卡住不该拖死 Agent，工具失败不该无限重试（尤其是副作用操作，重试前要考虑幂等性）。</li><li><strong>整任务预算</strong>：总步数上限、总 token 预算、总成本上限。这是防&quot;Agent 陷入循环无法自拔&quot;的最后手段——模型层可能永远不主动喊停，只有 harness 能数着步数强制终止。<strong>步数与 token 的双重预算比单一维度可靠</strong>：步数少但每步烧大量 token 的路径，单看步数发现不了。</li><li><strong>并发与限流</strong>：同一会话内并行工具数、同一账号并发会话数、对上游 API 的速率。模型倾向于一次开很多并行调用，harness 要按成本与上游承受力约束。</li></ul><h3 id="预算与-目标完成-的关系" tabindex="-1"><a class="header-anchor" href="#预算与-目标完成-的关系"><span>预算与&quot;目标完成&quot;的关系</span></a></h3><p>预算用尽时不能只报&quot;超预算了&quot;就完事——要区分两种情况：<strong>正常推进中预算用完</strong>（可恢复：加预算续跑）与<strong>空转耗尽预算</strong>（不可恢复：说明 Agent 在绕圈，恢复也白烧钱）。判断依据是最后 N 步是否有实际进展（文件变更、外部调用、状态推进）。<strong>预算耗尽报告里应该带上&quot;最后在做什么、卡在哪&quot;</strong>，让人能决定是续跑、换策略还是放弃——这是资源治理从&quot;计数器&quot;变成&quot;决策支持&quot;的关键一步。</p><h3 id="反例与陷阱" tabindex="-1"><a class="header-anchor" href="#反例与陷阱"><span>反例与陷阱</span></a></h3><ul><li><strong>只设单次超时，不设整任务预算</strong>：一次调用不会卡死，但任务会无限步数空转到天价账单。</li><li><strong>预算用提示词约束</strong>：&quot;请尽量在 10 步内完成&quot;没有任何强制力。预算必须由循环控制层计数并在代码里硬性终止。</li><li><strong>所有重试都无上限且立即重试</strong>：瞬时错误可以重试，但同一个错误反复出现说明策略错了，应该停下来换路径而不是继续烧钱。</li><li><strong>预算超限直接杀掉会话</strong>：没有把中间产物落盘就杀，恢复成本反而更高。资源治理要与 checkpoint 机制配合：预算告警时先做一次状态落盘，再决定终止与否。</li></ul><h2 id="agent-安全-内容进上下文之后的攻防" tabindex="-1"><a class="header-anchor" href="#agent-安全-内容进上下文之后的攻防"><span>Agent 安全：内容进上下文之后的攻防</span></a></h2><p>Agent 与普通程序最大的安全差异是<strong>它会把不受信任的内容读进上下文并据此行动</strong>：网页、文档、代码仓库、工具返回、邮件都可能包含诱导指令。提示层面的防御（告诉模型&quot;忽略注入&quot;）只是第一层且可被绕过；<strong>harness 层面的安全是把&quot;模型可能被诱导&quot;当作前提来设计</strong>——既然内容不可信、模型可能被操控，那就让被操控的模型也做不成坏事。</p><h3 id="工具注入的现实攻击面" tabindex="-1"><a class="header-anchor" href="#工具注入的现实攻击面"><span>工具注入的现实攻击面</span></a></h3><p>最典型的攻击链：恶意内容混在 Agent 读取的资料里 → 模型被诱导调用工具 → 工具产生真实副作用（读敏感文件、改代码、外发数据）。harness 的防御不依赖模型识破诱导，而是依赖执行层机制：</p><ul><li><strong>工具只暴露最小必要集</strong>：Agent 根本看不到的敏感操作，注入内容再强也无法触发——白名单机制是这一点的执行载体，敏感操作从可见范围内消失，比事后拦截可靠。</li><li><strong>参数白名单校验</strong>：即便模型被诱导调用&quot;删除文件&quot;工具，参数也要过路径边界校验，删不到白名单外。诱导只能让模型&quot;在规则允许的范围内&quot;动作。</li><li><strong>外部副作用默认高成本</strong>：凡是会离开沙箱的动作（网络外发、远端写入、安装全局依赖），默认进人工确认或直接禁止。网络外发尤其要拦——<strong>敏感数据泄露大多不是靠删库，而是靠悄悄外带</strong>。</li><li><strong>敏感内容标记与污染追踪</strong>：读入的文件标注来源（&quot;这是用户上传的不可信文件&quot;），让模型与后续裁决知道内容信任等级；工具结果若来自不可信输入，追加来源标记而不是让它冒充系统权威内容。</li></ul><h3 id="沙箱逃逸与权限提升" tabindex="-1"><a class="header-anchor" href="#沙箱逃逸与权限提升"><span>沙箱逃逸与权限提升</span></a></h3><p>沙箱隔离本身不是银弹，逃逸面主要在配置错误：</p><ul><li><strong>挂载泄漏</strong>：把宿主敏感目录（<code>/</code>、<code>~/.ssh</code>、docker socket）挂进沙箱，等于亲手打开门。挂载前问：模型真的需要读写这里吗？</li><li><strong>特权与内核共享</strong>：容器内 root + 共享内核 + 未禁用危险 syscall，就退化为&quot;带目录限制的普通进程&quot;。容器要配 non-root、capability drop、seccomp/用户态内核。</li><li><strong>资源耗尽型逃逸</strong>：沙箱内无限占 CPU/内存拖垮宿主机，需要配额兜底。</li></ul><h3 id="审计日志-安全事件的真相来源" tabindex="-1"><a class="header-anchor" href="#审计日志-安全事件的真相来源"><span>审计日志：安全事件的真相来源</span></a></h3><p>安全机制总会失效，审计日志是&quot;失效后还能查清&quot;的保障。Agent 审计日志的核心字段：会话与用户标识、时间线、<strong>每一步的完整决策链</strong>（模型输入摘要 → 模型输出 → 工具调用名与参数 → 裁决结果 → 执行结果）、批准人。工程要点：</p><ul><li><strong>记全不记糙</strong>：工具调用的参数必须记录——事后追查&quot;那条命令到底干了什么&quot;，参数比命令名有价值得多。敏感参数（密钥）要脱敏，但脱敏规则不能把关键证据一起抹掉。</li><li><strong>审计与可观测共用一份事件源</strong>：安全审计和排障 trace 是同一份运行时事件的不同视图。分开记录会导致&quot;安全日志里没有上下文、trace 里没有裁决信息&quot;，出事故时两边对不上。</li><li><strong>日志可写保护</strong>：沙箱内进程不能改写自己的审计日志，否则被攻破后日志会被清洗。日志要写到沙箱外的独立存储。</li></ul><h2 id="可观测性-把-黑盒决策-变成可回放、可归因的记录" tabindex="-1"><a class="header-anchor" href="#可观测性-把-黑盒决策-变成可回放、可归因的记录"><span>可观测性：把&quot;黑盒决策&quot;变成可回放、可归因的记录</span></a></h2><p>Agent 的每一步都是模型决策，失败时最常见的困局是&quot;看结果知道错了，但不知道是哪个决策、哪条上下文导致的&quot;。可观测性解决的就是这个问题：<strong>把每一步的输入、决策、动作、结果完整记录，让任何一次失败都能被回溯到具体一步</strong>。</p><h3 id="记录什么-决策轨迹而非结果" tabindex="-1"><a class="header-anchor" href="#记录什么-决策轨迹而非结果"><span>记录什么：决策轨迹而非结果</span></a></h3><ul><li><strong>每次 LLM 调用的完整输入与输出</strong>：system + 当时上下文（或至少其标识与摘要）+ 模型输出。没有调用输入，事后无法判断&quot;当时模型看到了什么导致它这么选&quot;。</li><li><strong>每次工具调用的声明与结果</strong>：工具名、参数、裁决结果、真实执行结果、耗时。工具调用是模型与世界的唯一触点，这条链是归因的主干。</li><li><strong>决策与决策之间的因果</strong>：为什么走到这一步（上一步的哪个结论触发了这次调用）。把工具结果与后续决策关联起来，才能区分&quot;是工具返回错&quot;还是&quot;模型用错了结果&quot;。</li></ul><h3 id="用-trace-把事件串成链" tabindex="-1"><a class="header-anchor" href="#用-trace-把事件串成链"><span>用 trace 把事件串成链</span></a></h3><p>单条日志无法支撑归因，需要把一次会话的所有事件组织成<strong>带层级与顺序的 trace</strong>：一次任务 = 一个 trace；一次 LLM 调用与其触发的工具执行 = 其中的 span。每个 span 携带开始/结束时间、父子关系与结构化属性。这样排查时能回答三类问题：<strong>这条链路花了多少时间（在哪一步最慢）、在哪一步开始偏离预期（对比成功案例的路径）、哪一步产生了实际副作用</strong>。</p><p>工程上常踩的坑：</p><ul><li><strong>只记结果不记决策</strong>：日志里只有最终产物，模型中间改了多少次主意、试过哪条死路全部丢失——这是 Agent 排障最痛的点。<strong>中间轨迹比结果有信息量</strong>。</li><li><strong>全量记录导致存储爆炸</strong>：每一步 LLM 调用的完整输入可能上万 token，任务一长就存不下。策略是&quot;结构化必存 + 内容分级存储&quot;：必存的是工具调用与裁决等小体积高价值事件；大体积的上下文快照按需抽样（如每 N 步存一次、失败会话必存）。</li><li><strong>丢失关联标识</strong>：没有统一的会话 ID / 请求 ID 贯穿 trace、审计、应用日志，事后无法把&quot;这笔账单&quot;&quot;这条命令&quot;&quot;这次模型调用&quot;对应到同一件事。</li></ul><h3 id="replay-与调试-让失败可复现" tabindex="-1"><a class="header-anchor" href="#replay-与调试-让失败可复现"><span>Replay 与调试：让失败可复现</span></a></h3><p>有了完整轨迹，就能做真正有效的调试：<strong>把记录下来的某一步输入原样重放给模型</strong>，验证&quot;同样的输入是否稳定产出同样的错误&quot;——若是，问题在模型或上下文；若重放不复现，问题在运行时（状态错乱、竞态）。更高阶的用法是<strong>离线修改轨迹做对照实验</strong>：同一段任务，换成不同的上下文组装、不同的工具描述，重放对比哪条路径收敛。这把 Agent 的调试从&quot;对着日志猜&quot;变成&quot;可重复的实验&quot;。采集 bad case 的标准动作因此是：失败时<strong>连同完整轨迹一起归档</strong>（含当时的上下文、工具链与裁决记录），而不是只留一句错误信息——没有轨迹的 bad case，等于一个无法复现的 bug 报告。</p><h2 id="收束-六块骨架如何咬合成一台机器" tabindex="-1"><a class="header-anchor" href="#收束-六块骨架如何咬合成一台机器"><span>收束：六块骨架如何咬合成一台机器</span></a></h2><p>回看整个 harness，六块机制不是各管一段，而是共享同一套事件与状态基础：checkpoint 复用 trace 的步骤边界，审计复用工具调用的事件记录，权限裁决结果同时进入审计与可观测，预算耗尽报告引用最后几步的轨迹。<strong>判断 harness 成熟度的试金石只有一件事：一个任务失败或越界之后，你能不能回答&quot;它每一步看到了什么、做了什么、谁批准的、花了多少钱&quot;</strong>——能回答，harness 就在正常工作；缺了任何一块，都会在某类事故里变成查无可查。</p><p>这一章从上到下走的是同一套设计逻辑：<strong>先定义边界（沙箱划清能碰什么），再定义裁决（权限决定能做什么），再定义连续性（状态保证中断后可恢复），最后用治理（预算）、安全（兜底）与可观测（留痕）把整个循环包成可上生产的产品</strong>。Harness 不改变模型的能力，它改变的是模型能力可以被信任的程度——这正是&quot;能跑通的 Agent&quot;与&quot;能交付的 Agent&quot;之间的全部距离。</p>`,79)]])}var s=r(a,[[`render`,o]]);export{i as _pageData,s as default};