import{a as e,i as t,u as n}from"./app-DRjtGc8k.js";import{t as r}from"./plugin-vue_export-helper-BDNMzG2s.js";var i=JSON.parse(`{"path":"/posts/notes/cs/ai.html","title":"AI Learning Notes","lang":"en-US","frontmatter":{"lang":"en-US","title":"AI Learning Notes","description":"AI mathematics, programming and theory","date":"2024-06-19T00:00:00.000Z","category":["Math"],"tag":["AI"],"head":[["script",{"type":"application/ld+json"},"{\\"@context\\":\\"https://schema.org\\",\\"@type\\":\\"Article\\",\\"headline\\":\\"AI Learning Notes\\",\\"image\\":[\\"\\"],\\"datePublished\\":\\"2024-06-19T00:00:00.000Z\\",\\"dateModified\\":\\"2026-09-23T16:34:13.000Z\\",\\"author\\":[{\\"@type\\":\\"Person\\",\\"name\\":\\"vexillium\\",\\"url\\":\\"https://vexillium2.github.io\\"}]}"],["meta",{"property":"og:url","content":"https://vexillium2.github.io/posts/notes/cs/ai.html"}],["meta",{"property":"og:site_name","content":"Vexilog"}],["meta",{"property":"og:title","content":"AI Learning Notes"}],["meta",{"property":"og:description","content":"AI mathematics, programming and theory"}],["meta",{"property":"og:type","content":"article"}],["meta",{"property":"og:locale","content":"en-US"}],["meta",{"property":"og:locale:alternate","content":"zh-CN"}],["meta",{"property":"og:updated_time","content":"2026-09-23T16:34:13.000Z"}],["meta",{"property":"article:tag","content":"AI"}],["meta",{"property":"article:published_time","content":"2024-06-19T00:00:00.000Z"}],["meta",{"property":"article:modified_time","content":"2026-09-23T16:34:13.000Z"}],["link",{"rel":"alternate","hreflang":"zh-cn","href":"https://vexillium2.github.io/posts/notes/cs/ai.html"}]]},"git":{"createdTime":1783438928000,"updatedTime":1790181253000,"contributors":[{"name":"vexillium2","username":"vexillium2","email":"jacobdu@JacobdeMacBook-Air.local","commits":3,"url":"https://github.com/vexillium2"}]},"readingTime":{"minutes":3.2,"words":961},"filePathRelative":"posts/notes/cs/ai.md","excerpt":"<h2>Machine Learning</h2>\\n<h3>Supervised Learning</h3>\\n<p>Supervised learning learns a <strong>mapping f from input X to output Y</strong> from a set of <strong>labeled examples</strong> (x_i, y_i). The label y is the <em>supervision signal</em>: it tells the model the expected answer for each sample, so training is essentially &quot;correcting the model by comparing its prediction y_hat = f(x) with the true y&quot;. The samples are assumed to be drawn i.i.d. from an unknown joint distribution P(X, Y), and the real goal is to minimize the <strong>expected loss</strong> E[L(f(X), Y)] over that distribution. Since P(X, Y) is unknown, we can only minimize the <strong>empirical risk</strong> on the finite training set — which is why supervised training tends to overfit, and why <strong>regularization</strong> (structural risk minimization, SRM) is usually required on top of pure ERM.</p>"}`),a={name:`ai.md`};function o(r,i,a,o,s,c){return n(),t(`div`,null,[...i[0]||=[e(`<h2 id="machine-learning" tabindex="-1"><a class="header-anchor" href="#machine-learning"><span>Machine Learning</span></a></h2><h3 id="supervised-learning" tabindex="-1"><a class="header-anchor" href="#supervised-learning"><span>Supervised Learning</span></a></h3><p>Supervised learning learns a <strong>mapping f from input X to output Y</strong> from a set of <strong>labeled examples</strong> (x_i, y_i). The label y is the <em>supervision signal</em>: it tells the model the expected answer for each sample, so training is essentially &quot;correcting the model by comparing its prediction y_hat = f(x) with the true y&quot;. The samples are assumed to be drawn i.i.d. from an unknown joint distribution P(X, Y), and the real goal is to minimize the <strong>expected loss</strong> E[L(f(X), Y)] over that distribution. Since P(X, Y) is unknown, we can only minimize the <strong>empirical risk</strong> on the finite training set — which is why supervised training tends to overfit, and why <strong>regularization</strong> (structural risk minimization, SRM) is usually required on top of pure ERM.</p><p>Training is a closed loop: forward pass → compute the loss → backpropagate gradients → update parameters with an optimizer, repeated over many epochs. The loss is chosen to match the task; under an explicit assumption about the noise/model it coincides with the <strong>negative log-likelihood</strong> derived from maximum likelihood estimation (MLE).</p><p>Supervised tasks are usually divided by the type of label:</p><ul><li><p><strong>Regression</strong> — Y is continuous. The model outputs a real number, trained with a continuous loss such as MSE or MAE. Typical algorithms: linear regression, decision trees / GBDT, neural network regressors. Applications: house-price, temperature or sales forecasting.</p></li><li><p><strong>Classification</strong> — Y is a discrete category. It covers binary, multi-class and multi-label settings; the output layer converts raw scores (logits) into probabilities via sigmoid (independent labels) or softmax (mutually exclusive classes), trained with cross-entropy. Typical algorithms: logistic regression, SVM, k-NN, tree ensembles, neural networks. Applications: spam detection, image recognition, sentiment analysis.</p></li></ul><p>Labels are not limited to a scalar: <em>structured-output</em> tasks such as sequence labeling, machine translation and object detection also fit the supervised paradigm.</p><p>Representative algorithms span classical methods (linear/logistic regression, SVM, k-nearest neighbors, decision trees and their ensembles such as random forest and GBDT) and deep networks (MLP, CNN, RNN, Transformer-based models). What unifies them is the training paradigm, not the architecture: the same network can be trained in a supervised way on one task and unsupervised on another.</p><p>Common misconceptions:</p><ul><li><p><strong>&quot;Supervised&quot; describes the training data/paradigm, not the model.</strong> In modern deep learning a model is usually first <em>self-supervised</em> pretrained on unlabeled text or images, then <em>supervised</em> fine-tuned on labels — the architecture never changes.</p></li><li><p><strong>Label quality sets the performance ceiling.</strong> Supervision is the only signal carrying information about the target; noisy or mislabeled data directly caps accuracy and cannot be fully compensated by a larger model.</p></li><li><p><strong>The i.i.d. assumption can silently break.</strong> Supervised learning assumes deployment data follows the training distribution. Under distribution shift or concept drift, validation accuracy no longer predicts production behavior.</p></li></ul><h3 id="unsupervised-learning" tabindex="-1"><a class="header-anchor" href="#unsupervised-learning"><span>Unsupervised Learning</span></a></h3><p>Unsupervised learning handles data with <strong>no labels — only inputs X</strong>. Instead of predicting a target, it aims to discover structure: groups, low-dimensional representations, or the shape of the underlying distribution P(X). From an information-theoretic viewpoint, supervised learning estimates the conditional distribution P(Y|X), while unsupervised learning compresses and summarizes P(X) itself — the model receives no &quot;right or wrong&quot; feedback at all.</p><p>Typical problem families and representative algorithms:</p><ul><li><p><strong>Clustering</strong> — partition samples so that points in the same group are similar and points in different groups are dissimilar. Algorithms: K-means (prototype-based, favors spherical clusters), hierarchical clustering, DBSCAN (density-based, handles arbitrary shapes and noise), Gaussian Mixture Models (soft, probabilistic assignment). Applications: customer segmentation, image compression, grouping related documents.</p></li><li><p><strong>Dimensionality reduction &amp; representation learning</strong> — map data into a lower-dimensional space while preserving as much information as possible. Linear: PCA / SVD (keep the directions of maximum variance); visualization-oriented: t-SNE, UMAP; neural: autoencoders (minimize reconstruction error). Uses: feature compression, denoising, visualization, and generating embeddings for similarity search.</p></li><li><p><strong>Density estimation &amp; anomaly detection</strong> — explicitly model P(X) (e.g., GMM, kernel density estimation); points falling in very low-probability regions are flagged as anomalies or outliers.</p></li><li><p><strong>Self-supervised learning — the blurred line in the deep learning era.</strong> What is loosely called &quot;unsupervised pretraining&quot; today is usually <em>self-supervised</em>: the data itself generates pseudo-labels via a pretext task (e.g., BERT&#39;s masked language modeling, contrastive learning), and the model is then trained with ordinary supervised machinery. This learns representations without human annotation, but it is not the classic &quot;discover the structure of P(X)&quot; setting — a distinction that is easy to confuse and frequently asked about in interviews.</p></li></ul><p>Difficulties and common misunderstandings:</p><ul><li><p><strong>No ground truth ⇒ no universal metric.</strong> Without labels, &quot;error&quot; is undefined, so the model cannot be scored by accuracy. Quality is judged only by internal heuristics (within-cluster SSE, silhouette score, reconstruction loss), downstream task performance, or manual inspection — which is why unsupervised results are harder to compare objectively.</p></li><li><p><strong>A clustering result is not the &quot;true&quot; grouping of the data.</strong> The partition depends on the chosen distance metric, feature representation, and hyperparameters such as the number of clusters K. The same dataset can yield several different but equally useful groupings depending on the goal — clustering outputs are <em>useful</em>, not <em>correct</em>.</p></li><li><p><strong>Unsupervised learning creates no information.</strong> It can only expose structure already implied by the data and the chosen features. With too little or unrepresentative data it still underfits or overfits (e.g., a large K in K-means can fit pure noise). And since there are no labels, it cannot directly answer a predictive question: to predict an unseen target, labels must be introduced, which is why unsupervised methods are usually combined with a later supervised or self-supervised stage.</p></li></ul>`,16)]])}var s=r(a,[[`render`,o]]);export{i as _pageData,s as default};