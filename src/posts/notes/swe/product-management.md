# 产品与软件项目笔记





## 产品的生命周期

1. 产品需求分析

    1. 对过往需求分析和市场研究进行阶段性总结

    2. 转化为上下游都看得懂的产品设计说明

2. UED

3. 产品开发

4. 产品测试

5. 产品运维

# 产品方向

### 战略与用户理解

用户画像 Persona

描述目标用户是谁，包含动机、痛点、行为

用户旅程地图 User Journey Map

用户全程体验路径，情绪曲线、痛点标注

竞品对比图 Competitive Analysis

## 产品项目规划

> 产品的宏观设计
> 
> 

## 产品设计文档PRD

> 产品的微观设计
> 
> 

并非所有情况都需要PRD，小修小补可以使用任务系统解释需求（Task design）；新的产品可能难以用传统方法解释清楚，所以可以采用讲故事的方法解释（Story Design）。

对于正式的PRD，可以拆分为：

- 背景

- 收益

- 方案

- UE图

- 埋点

## 项目从设计到落地



### 项目准备

#### 从产品、商业角度思考

产品化：背景意义、测试性能、方案对比（场景）

#### 从技术角度思考

业务流程图        数据流图

数据\-实体图

时序图

技术选型，从以下角度思考

- 技术

- 版本

**阶段性结果：xx技术市场格局调研图/报告、技术与业务数据量匹配图**

#### 技术开发规划

优先级规划

1. 基本框架确定

2. 数据库优先连接搭建测试环境

3. 

https://www\.feishu\.cn/community/article?id=7400690886809485313

## 

#### 项目1

1. 为解决复杂题目的嵌套结构（如多级选项）存储，便于封装Prompt，使用JSON字段存储题目元数据，同时为高频查询字段添加索引，百万数据量下查询响应时间从2000ms降至300ms。

2. 设计可插拔的评分架构，基于策略模式\+自定义注解实现了多种评分算法（如统计得分、AI评分），实现策略类自动注册，通过反射扫描注解，策略扩展效率提升60%以上。

3. 基于 RxJava 实现 AI 评分流式处理，通过组合操作符链式调用处理 AI 异步数据，再通过括号平衡算法准确拼接出单道题目，使得逻辑简单清晰。

4. 为防止用户多次提交重复答案，基于雪花算法为每次答题分配唯一id，并利用数据库主键实现幂等设计，避免了重复提交脏数据。

5. 引入 Caffeine 本地缓存机制，避免重复 AI 调用，提高评分性能（10s→5ms）的同时大幅节约token成本；并通过 Redisson 分布式锁解决缓存击穿问题。

1\.如何接入AI？

2. 什么是SSE？怎么在项目中实现SSE？什么是RxJava和反应式编程?
//3\. 什么是微服务?什么是服务治理?什么是服务熔断?什么是服务降级?什么是服务限流?什么是服务幂等?

3. 如何设计库表的？字段、索引、关联

4. 为什么用缓存？缓存击穿是什么？还有什么办法解决？

#### 项目2 VRPC

基于 Java \+ ZooKeeper \+ Vert\.x \+ 自定义RPC协议 实现，通过注解和配置文件快速使用框架；支持通过 SPI机制动态扩展序列化器、负载均衡器等模块。

主要工作:

1. 核心架构：包括消费方调用、多种序列化器、网络服务器、请求处理器、服务注册器模块。

2. 注册中心优化：利用 Curator 框架构建基于 ZooKeeper 的注册中心，实现服务发现本地缓存\+节点监听机制，降低对注册中心的访问频率，提升系统服务可用性与调用性能。

3. 消费者服务缓存：使用本地对象维护已获取到的服务提供者节点缓存，相比于每次从注册中心获取，性能显著提高（900ms→1ms）；并通过 ZooKeeper 的watch 机制，监听节点的过期并自动更新缓存。

4. 定义序列化器接口，实现了基于 JSON、Kryo 和 Hessian 的序列化器，并通过 ThreadLocal 解决了 Kryo 序列化器的线程安全问题。

5. 由于 HTTP 协议额外头部信息较多，基于 Vert\.x TCP 服务器\+ 类 Dubbo 的紧凑型消息结构（字节数组）自实现了 RPC 协议，提升网络传输性能。

容错机制

故障转移 

1. 什么是RPC，你的RPC和其他有什么不同？介绍一下项目？

2. 介绍整个系统的核心架构设计？

3. 什么是注册中心?怎么利用注册中心实现服务发现?

4. 为什么使用ZooKeeper？

5. 如何实现网络通信？为什么使用Vertx？

6. 在项目中用到了哪些设计模式？
工厂模式、单例模式、策略模式、装饰者模式

7. 什么是序列化和反序列化？在项目中如何处理？

8. 什么是TCP沾包问题？在项目中如何解决？

9. 介绍一下项目难点？如何解决？如何优化？

10. 首先，我认为处理TCP沾包半包问题是个难点，为了解决粘包半包的问题，我使用了Vert\.x 内置RecordParser。但由于消息体的长度是不固定的，所以我
要通过调整 RecordParser 的固定长度（变长）来解决，将读取完整的消息拆分为 2次，先获取请求体的长
度，再根据长度完整读取请求体。
由于代码较为复杂，我运用了装饰者模式，新写一个 Wrapper 类来实现上述逻辑。使用时只需要用 Wrapper 
封装原有Vert\.x 的TCP 处理器，就能解决粘包半包。不用修改原始处理器，使得系统能够更灵活地扩展，代码更利于维护。

11. 介绍一下项目描述？
VRPC 是我

12. 什么是负载均衡？负载均衡算法有哪些？
负载这里就是指对对注册中心网络请求，为了避免单一注册中心承受过大的负载导致性能下降或者不可用，确保每个注册中心都能有效处理请求，提高整体系统并发量，
轮询、随机、加权轮询、加权随机、最小连接数（长连接）、IP Hash（同一IP一直使用同一个注册中心，保持会话一致性）、一致性Hash



1. 你是如何保证用户上传的数据文件的安全性的？

2. 什么是限流？有哪些常见的限流算法？

3. 什么是Redisson的RateLimiter？它在项目中具体如何实现分布式限流？你的限流策略是什么?

4. 



## 项目风险管理

什么是？为什么重要？

涉及的要素和风险管理计划的内容

常见的风险来源

风险识别过程、有助于识别项目风险的工具和技术，以及风险识别的主要输出——风险登记册

定性风险分析过程，解释如何创建概率/影响矩阵，应用“十大风险项跟踪”技术，并使用专家判断对风险进行排序

解释定量风险分析过程以及如何应用决策树来量化风险

讨论风险应对规划以及风险监控和控制所涉及的内容

## 商业模式1



广告联盟和互联网

## 范例

### 飞书笔记Agent Cli

简单叙述

本产品是建立在飞书Agent\-开源Lark CLI上的improvement，针对文档这一块增加不同文档使用场景下的ai协助功能，包括自动整理知识库模块、出题模块、书写助手模块、自动

#### 战略与用户理解

用户画像

用户为有笔记博客、记录、报告等需求的工作、学习人群。

用户旅程地图

竞品对比图

#### 信息架构与流程设计

信息架构图

业务流程图

用户故事地图

#### 需求与界面设计

原型图

交互说明文档

页面流程图

#### 优先级、规划与度量

优先级矩阵

产品路线图

数据漏斗/指标树



