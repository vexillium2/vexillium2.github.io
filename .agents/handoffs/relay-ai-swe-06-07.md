# 接力任务：扩写 `06-harness.md` 与 `07-eval.md`

你是接手 AI 应用开发笔记系列写作的 agent。系列目录（实际路径）：`src/zh/posts/notes/ai-swe/`。此前 01（LLM 基础）、02（Prompt 工程）、03（RAG）、04（Agent 智能体）、05（工具调用与 Tool Engineering）已按规范完成扩写，均可作风格范本；本棒处理 06 与 07，完成后把本交接文件替换为下一棒任务。

## 0) 必读文件（按顺序）

1. 写作方法论（最重要，严格执行）：`.agents/swe-tech-blog/SKILL.md`
2. 目标文件当前提纲：`src/zh/posts/notes/ai-swe/06-harness.md`、`src/zh/posts/notes/ai-swe/07-eval.md`
3. 已完成范本（先通读感受结构、深度与口吻）：`src/zh/posts/notes/ai-swe/01-llm.md` ~ `05-tool.md` 共五篇，尤其 04（Agent 运行时/上下文工程/失败模式）与 05（工具协议/安全模型）与本棒内容相邻
   - **注意**：06 与 04、05 相邻（04 已讲 Agent 循环/上下文窗口组装/审批分级，05 已讲工具设计/MCP 安全模型/网关治理）。动手前务必读 04、05，避免把平台层话题当新内容重复长讲；07 评估是全新主题，无相邻章节，可放开写。
4. 旧版素材（可提炼复用、勿照搬其结构）：`src/zh/posts/notes/ai-swe/ai-swe.md`

## 1) 任务

把 `06-harness.md`、`07-eval.md` 从"提纲清单"扩写为完整技术文章，覆盖提纲全部知识点。允许按内容内在逻辑合并/重排小节，但不允许删点。

- **06（Agent Harness 工程）**：Harness 架构（以主流编码 Agent 为样本拆解）；沙箱执行（容器 / microVM / WASM、文件系统与网络隔离策略）；权限系统（工具白名单、危险操作分级审批、最小权限落地）；会话与状态管理（checkpoint、断点续跑、多会话隔离）；上下文管理工程（compaction/summarization、大文件读取策略、todo 外置）；资源治理（token 预算、超时、并发限制、成本控制）；Agent 安全（工具注入、沙箱逃逸、审计日志）；可观测性（trace、每步决策的 replay 与调试）。
- **07（Agent 评估）**：为什么 Agent 评估比传统 ML 评估难（非确定性、轨迹长、无标准答案）；评估分层（单步 → 轨迹 → 端到端）；LLM-as-Judge（评估 prompt 设计、位置偏差、与人工一致性校准）；主流 benchmark 解读；RAG 专项评估；自建评估集（黄金样本、bad case 回流、回归测试）；在线评估（A/B、用户反馈、灰度）；CI 化的 Agent 测试。

### 与已完成章节的边界（交接协调用，正文禁止出现任何跨章引用）

- **06.01/06.05 上下文管理工程**：04 已从"Agent 如何组装自己的上下文窗口"写过预算分层、静态前缀、压缩与截断；06 落到 **harness 的平台机制**——compaction 触发与实现策略、超长文件分段读取、todo 等运行态外置到上下文之外，视角是"工具链怎么替模型省窗口"，不是"窗口内怎么排布"。
- **06.02/06.03/06.07**：05 已从"协议与权限侧"讲过 MCP 安全模型、授权在 host、审计与网关；04 已讲过危险操作分级审批的"思路"。06 要写的是 **harness 层的落地实现**：沙箱技术选型与逃逸面、权限在运行时怎么强制执行、审计日志的链路设计、工具注入的具体攻击面——落到系统机制，别再讲"授权该由谁决策"的原则层面。
- **06.06 资源治理**：04 边界说明里明确把"平台资源预算/超时/并发"从 04 剔除，属 06 主场，可展开。
- **07 各节**：LLM-as-Judge 与评估 prompt 会碰 prompt 工程，但视角是"如何评估"，不是"如何写提示词"；与 02 的生成侧技巧不冲突，注意别把评估 prompt 技巧写成通用 prompt 教程。
- 章节示例会点名具体产品/框架（Claude Code 等）与 benchmark（SWE-bench 等），只作**样本与参照物**，描述其机制与边界即可，不写成产品说明书；benchmark 数据/榜单属易过时信息，按"测什么能力、怎么看结果"处理。

## 2) 用户明确约束

- 用**正常 md 分段**（##/###），不要沿用 "06.01 / 07.0x" 编号做小标题，也不要保留原提纲行。
- 面试热点**融入对应小节**，禁止单列"面试题"章节。
- 禁止跨章节/跨文件引用（如"（见 xx 章）"、"详见工具章节"）。各章保持自包含。
- 技术表述准确；风格是"真正理解技术的 SWE 在整理笔记"，不堆术语、不做百科式铺陈；关键结论/定义句加粗；表格仅在多对象横向比较时使用（如沙箱技术对比、评估分层适合用表）。
- frontmatter 保持现状（含 `date: 2026-09-07`），不要改。
- **反例意识**：不适用/不该用的场景（如何时不必要上沙箱、何时评估指标会骗人）比罗列优点更有信息量，两章都要有真实工程的取舍，不是机制罗列。

## 3) 工具使用技巧（来自前几棒的血泪教训）

1. **别用一次 `Write` 写完整篇长文**——单条消息输出有长度上限，会被静默截断。做法：先 `Write` 首块（原样保留 frontmatter + 引言 + 开头小节），之后用 `SearchReplace` 逐段**追加**，每段 `new_str` 控制在 ~4000 字符内；`old_str` 用文件末尾唯一一行/几句作锚点。
2. 修改前必须 `Read` 目标文件；`Write` 覆盖前也必须先 `Read`。
3. 同一文件末尾的追加必须**串行**；不同文件的编辑可并行。
4. 完成后自检：`Grep`（`^#{2,3}`）+ `wc -l` 确认无截断、无残留提纲编号、末行完整结束；确认全文无"面试题"独立章节、无跨章引用；确认没把"（本节未完）""分两次追加"这类过程性注释漏进正文。
5. 一旦发现"用户描述与现实不符"或方向有歧义，先用 `AskUserQuestion` 给出选项确认，再动笔，不要擅自假设。
6. 长任务用 `TodoWrite` 拆步跟踪（每章一组、加最终自检项）。
7. 易过时的事实（benchmark 榜单、价格、具体版本号、厂商改名）写成"该看什么维度 / 如何自测"的方法论，不写死结论。
8. 查内容：精确找文件/符号用 `Grep`/`Glob`/`Read`；开放式的"某主题散落在哪些文档"用 `SearchCodebase`；需要 shell（如 wc 校验）才用 `RunCommand`。git 操作除非被明确要求，不做 commit/push。
9. 不要把交接/规划文档写进 `src/zh/posts/` 下——会被站点构建为正文。

## 4) 交付前自检清单

- [ ] 覆盖提纲全部知识点，无删点
- [ ] `##`/`###` 层级正确，无 "0x.xx" 编号样式残留
- [ ] 无独立"面试题"段落（已融入小节）
- [ ] 无跨文件/跨章引用；无对 01-05 已讲内容的长篇复述
- [ ] 文件完整收尾、无截断、无过程性注释残留
- [ ] frontmatter 未改动

## 5) 系列状态（本棒完成后给下一棒留档）

已完成：01 LLM 基础、02 Prompt 工程、03 RAG、04 Agent 智能体、05 工具调用与 Tool Engineering。
本棒：06 Agent Harness 工程、07 Agent 评估。
剩余待扩写（仍在仓库，勿动）：08 框架、09 微调、10 多模态、11 综合面试。后续接棒时新建类似本文件的交接书（命名形如 `relay-ai-swe-08-09.md`），本文件在完成 06/07 后即可替换删除。
